# CLAUDE.md — 십로케이션 (호선 위치 웹)

> 김은호(HSGINS 목포 현장 LNG 펌프타워 공정관리자) 개인 도구.
> LNG 공정관리 앱 레포에서 **피 흘려 얻은 규칙**을 옮겨왔다. 특히 "화면이 뭉개진다" 계열은
> 그쪽에서 7번 시도 끝에 root cause 를 잡은 것이라, 같은 삽질을 여기서 반복하지 말 것.
> 작성 2026-08-28.

---

## 👤 사용자 / 작업 방식

- 용어·약어는 전부 **조선·해양 기준**으로 읽는다 (탑재·핏업·취외·이머전시·G/T·D/L·안벽·도크).
- **답변은 한국어.**
- **수정이 끝나면 묻지 말고 배포한다** — typecheck·build 통과 시 커밋 → push 까지 자동.
  파괴적 작업(삭제·이력 변경·프로덕션 데이터 수정)만 확인받는다.

---

# ⚡ TOP-PRIORITY RULES

### 0. 스스로 한다 ⭐⭐⭐
**클로드가 스스로 할 수 있는 건 최선을 다해 연구해서 스스로 한다.**
사용자에게 뭔가 해달라고 하기 전에 반드시 먼저 따진다:
- 연결된 도구로 되나? (Vercel MCP · Supabase MCP · GitHub MCP · 웹검색)
- 깃허브 액션으로 되나? (크론·수동실행·시크릿 — 이 레포 SA 가 이미 GCP 인증을 갖고 있다)
- 실패해도 스스로 진단할 수 있게 만들 수 있나? (실패 시 스크린샷·HTML 을 아티팩트로 남겨
  다음 세션이 사용자 없이 고치게)
사용자에게 남기는 건 **정말 사용자만 할 수 있는 것**뿐이다: 비밀번호를 시크릿에 넣는 것,
계정 권한 부여(GitHub App 설치 등), 실물 확인. 그것도 "왜 나만 할 수 있는지" 한 줄과
정확한 클릭 경로를 함께 준다.
(2026-08-29 실제로 혼났다: 정찰 스니펫 실행·룰 배포를 사용자에게 미뤘는데
 둘 다 액션과 MCP 로 스스로 할 수 있는 일이었다.)



> Tradeoff: bias toward caution over speed. 사소한 건 알아서 판단.

### 1. Think Before Coding
추측 금지. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.
- 가정은 명시한다. 불확실하면 묻는다.
- 해석이 여러 개면 다 제시한다 — 조용히 하나 고르지 말 것.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 뭔가 불명확하면 멈춘다. 뭐가 헷갈리는지 이름 붙이고 묻는다.

### 2. Simplicity First
문제를 푸는 최소 코드. 투기적인 것 금지.
- 요청하지 않은 기능 금지. 1회용 코드에 추상화 금지.
- 요청 안 한 "유연성"·"설정 가능성" 금지. 불가능한 시나리오의 에러 처리 금지.
- 200줄 썼는데 50줄로 되면 다시 쓴다.
- *"시니어 엔지니어가 이걸 과설계라고 할까?"* → 그렇다면 단순화.

### 3. Surgical Changes
건드려야 할 것만 건드린다. 내가 만든 것만 치운다.
- 인접 코드·주석·포매팅 "개선" 금지. 안 망가진 것 리팩터 금지.
- 기존 스타일에 맞춘다. 내 취향과 달라도.
- 관계없는 죽은 코드는 *언급*만, 삭제하지 말 것.
- **테스트: 바뀐 모든 줄이 사용자 요청으로 직결되는가.**

### 4. Goal-Driven Execution
성공 기준을 먼저 정하고, 검증될 때까지 돈다.
- "버그 고쳐" → "재현하는 테스트를 먼저 쓰고, 그걸 통과시켜라"
- 다단계면 시작 전에 `[단계] → 검증: [확인방법]` 을 나열한다.

---

## 🔍 제0원칙 — 코딩 전 인터넷 자료조사

**모든 작업 시작 전, 인터넷에서 깊이 조사한 후에 코드를 만진다.**

- **표면 1회 검색 금지.** 첫 결과에 만족하지 말 것.
- 목표: 같은 증상의 GitHub issue + 공식 docs + Chromium/WebKit 버그트래커 + StackOverflow
  **최소 3 source** 교차 확인 후 root cause 가설 확정.
- fix 코드 쓰기 전: "이 fix 가 어떤 source 의 어떤 사례와 매칭되는지" 한 줄 보고.
- **5번 이상 시도해도 안 풀리면 root cause 가설 자체가 틀렸다는 신호** — 더 시도하지 말고
  조사로 돌아간다. (실제로 폰트 흐림에서 5+ 시도 전부 실패한 원인이 조사 부족이었다.)
- OS/브라우저별 버그는 **어디서 재현되고 어디서 안 되는지 먼저 확인**하고 그 조합으로 검색
  ("Chrome Windows blurry canvas" ≠ "Chrome macOS").

---

## 🚨 디버깅 제1원칙: 비교 관찰 (Compare-First)

**버그를 잡을 때 가장 먼저 할 일은 "잘 되는 곳과 안 되는 곳의 차이를 비교"하는 것.** 추측보다 항상 우선.

### 실패한 접근 (절대 반복 금지)
안 되는 것만 들여다보며 가설 → 수정 → 실패 → 다른 가설 → 반복. 이 사이클로 한나절을 날린 적 있다.

### 올바른 순서
1. **잘 작동하는 유사물을 찾는다.** ("이 앱/다른 앱에 비슷한 게 또 있나? 그건 선명한가?")
2. **잘 되는 것과 안 되는 것을 직접 diff 한다** (DOM 구조, CSS, 렌더 경로, 스케일 배수).
3. **차이점이 곧 root cause 후보다.** 추측이 아니라 증명된 후보.
4. 그 차이를 안 되는 쪽에 적용해 검증.

**"X 는 선명한데 Y 는 뭉개짐" 이 가장 강력한 단서다.** 지도가 뭉개지면 먼저 물어라 —
*확대했을 때만인가, 100% 에서도인가? 텍스트만인가, 선/도형도인가? 특정 브라우저·기기만인가?*
이 셋이 갈리는 지점이 곧 원인이다(아래 표).

---

# 🗺️ 지금의 블로커: 배경지도가 뭉개진다

## 먼저 증상을 3갈래로 가른다 (여기서 원인이 거의 결정된다)

| 증상 | 유력 원인 |
|---|---|
| **확대할수록** 뭉개짐, 100%에선 멀쩡 | 배경이 **래스터(PNG/JPG)**. 원본 해상도 이상으로 늘리는 중 → 보간 blur |
| **100%에서도** 전체가 살짝 흐림, 특히 고DPI 노트북 | **canvas devicePixelRatio 미적용**, 또는 비정수 px 위치 |
| **텍스트만** 흐림, 선·도형은 선명 | **GPU layer 강제** (`will-change`, `translate3d`) → 서브픽셀 렌더링 차단 |

## ★정답: 배경지도는 래스터가 아니라 **SVG(벡터)** 다

야드 배치도(안벽·도크·크레인·건물)는 본질적으로 **도면**이다. 도면을 PNG 로 만들면
확대할 때 반드시 뭉개진다 — 해상도를 아무리 올려도 한계가 있고 파일만 무거워진다.

- **PDF/CAD 원본이 있으면 벡터로 뽑는다**: PDF → SVG (`pdf2svg`, Illustrator, Inkscape).
  절대 PDF → PNG 로 가지 말 것.
- 원본이 없어 새로 그려야 하면 → **클로드디자인에 SVG 로 그리게 한다** (아래 브리프).
- SVG 면 확대해도 선이 수학적으로 다시 그려져 **영원히 선명**하다.

### SVG 로 갈 때 같이 지켜야 하는 것
- 확대/축소는 **`viewBox` 를 바꿔서** 한다. `transform: scale()` 로 늘리지 말 것
  (스케일은 래스터든 텍스트든 뭉갠다).
- 선은 `vector-effect: non-scaling-stroke` — 안 주면 확대할 때 선이 같이 굵어져 뭉툭해진다.
- 라벨(호선번호·안벽명)은 **SVG `<text>`** 로. 이미지에 구워 넣으면 그 순간 래스터다.
- 아주 축소했을 때 라벨이 뭉치면 **글자를 줄이지 말고 숨긴다** (줌 단계별 표시/숨김).

### 그래도 래스터(위성사진 등)를 깔아야 한다면
- **타일**로 쪼갠다(줌 레벨별 이미지). 한 장짜리 큰 PNG 를 늘리는 건 답이 없다.
- `<img>` 로 깔 때 CSS 크기 = 원본 픽셀의 정수배로 맞춘다.
- `image-rendering` 은 상황별로: 사진 = `auto`, 도면 스캔 = `crisp-edges` 실험.

### canvas 를 쓴다면 (마커·궤적 오버레이 등)
```js
const dpr = window.devicePixelRatio || 1;
canvas.width  = cssW * dpr;      // 실제 버퍼
canvas.height = cssH * dpr;
canvas.style.width  = cssW + 'px';  // 화면 크기
canvas.style.height = cssH + 'px';
ctx.scale(dpr, dpr);             // ★이 줄이 빠지면 고DPI 에서 전부 흐리다
```
`ctx.scale` 을 빼먹는 게 "다 뭉개져" 의 가장 흔한 원인이다. 창을 다른 모니터로 옮기면
dpr 이 바뀌므로 **resize 때 다시 계산**해야 한다.

---

## 🔒 흐림 회귀 방지 인바리언트 (LNG 앱에서 그대로 가져옴 — 실측으로 얻은 것)

### `will-change: transform` 절대 박지 말 것 ⭐⭐⭐
- LNG 앱에서 **6 커밋을 흐림 잡는 데 날리고** 7번째에 찾은 root cause가 이것이다.
  GPU layer 를 영구 promote → 자손 텍스트 **서브픽셀 렌더링 차단** → Windows ClearType
  무력화 → 줌하면 fuzzy.
- **pan/zoom 지도는 이 함정에 정확히 걸리는 구조다.** "성능 최적화" 랍시고 뷰포트에
  `will-change: transform` 을 넣는 순간 지도 위 모든 글자가 흐려진다.
- **새 GPU hint 추가 금지** — `transform: translate3d(0,0,0)`, `will-change: *`,
  `backface-visibility: hidden` 전부 같은 원인.
- trade-off: 팬 fps 가 미세하게 떨어질 수 있다. **사용자는 "샤프한 글씨" 를 우선한다.**
  요즘 브라우저는 어차피 자동 promote 라 실제 성능 차이는 거의 없다.

### `:hover` 에 transform 금지 ⭐
- `:hover { transform: translateY(-1px) }` 같은 lift → 줌 상태에서 폰트 흐림 +
  hover 를 벗어나도 GPU layer 가 남아 계속 흐림.
- 호선 마커 hover 효과는 **box-shadow 진하게 + `filter: brightness(1.04)` 만**.

### pan/zoom 뷰포트 paint 격리
```css
.map-viewport { isolation: isolate; }   /* iOS Safari 팬/줌 시 잔상 차단 */
.map-marker   { contain: layout paint; }
```
- `contain: strict` 는 과하다 — 마커의 라벨/화살표가 박스 밖으로 나갈 때 잘린다.

---

# 📍 데이터: 세이프티원 3중점검 → 십로케이션

세이프티원 사이트 **3중점검 탭**에 호선 위치 지도가 있다. **배가 실제로 이동하면 2~3시간
안에 갱신**돼서, 어느 호선이 어디로 갔는지 찾기 쉽다. 이걸 십로케이션의 위치 소스로 쓴다.

## ★2026-08-28 확인 완료 — 지도에서 좌표는 못 가져온다

세이프티원 3중점검 지도는 **`<canvas>` 다** (실측: `tag: 'CANVAS'`, 1793×1560).
그림을 픽셀로 찍어놓은 것이라 구역·배가 개별 요소로 들어있지 않다.
**DOM 에서 좌표를 뽑는 길은 없다.** 아래 "위경도냐 픽셀이냐" 조사는 끝난 얘기다.

대신 **화면 아래 `리스트 보기` 표**에 `호선` / `위치` 가 텍스트로 있다
(예: `8300` / `2도크 > TANK > 1TANK2`, `8207` / `2안벽 > CARGO TANK`).
위치는 여기서 가져온다. 지도 파싱은 시도하지 말 것.

남은 문제는 **`2안벽` 같은 구간 이름을 지도 좌표로 바꾸는 표**를 만드는 것이다.
한 안벽에 배가 여러 척 붙으므로, 리스트가 구간 안 순서까지 주는지 실제 표를
펼쳐서 확인해야 한다.

또 하나 — 캔버스 실제 픽셀(1793×1560)이 화면 표시 크기(2210×2126)보다 **작다**.
원본이 이미 늘려서 그려지는 중이라 확대해도 흐리다. 더 선명한 원본이 필요하면
세이프티원에서 **지도를 확대한 상태로 나눠 여러 장** 뽑아야 한다.

### 캔버스를 PNG 로 뽑는 법 (콘솔 한 줄)
```js
(()=>{const c=document.querySelector('canvas'),a=document.createElement('a');
a.download='safetyone-map.png';a.href=c.toDataURL('image/png');a.click();})()
```

## ★2026-08-29 확정 — 선석 구조: 돌핀이 안벽을 둘로 가른다

세이프티원 원본에서 안벽에 붙은 배를 x 순서로 훑으면 무리가 넷이고, **각 돌핀이 자기
안벽의 두 선석 사이에 정확히 끼어 있다.** (아래 x 는 새 지도 좌표계 1380x840 기준)

| 선석 | x 범위 | 그날 붙어 있던 배 |
|---|---|---|
| 2안벽 B | 5~150 | 8206, 8285 |
| **2돌핀** | 157 | 8222, 8282, 8203 (잔교에 매달림) |
| 2안벽 A | 178~320 | 8207, 8208 |
| 1안벽 B | 475~600 | 8254, 8246 |
| **1돌핀** | 648 | 8314, 8315 |
| 1안벽 A | 710~845 | 8262, 8263 |
| 1BERTH | 953~979 | 8209, 8238 |
| 플로팅 | 957~974 | 8313 |

### ★선석 이름은 `berth` 에 세부까지 적는다 — `1안벽 A선석` (2026-09-04 사용자 지시)
- 공정관리비서가 `ships/{호선}.berth` 문자열을 **그대로 찍는다**(공정표 📍 배지 + 자재 통보
  「배송지: 1안벽 8254호선」 줄). 그래서 여기가 틀리면 잘못된 선석이 그대로 현장에 나간다.
- 세이프티원은 선석 이름을 안 준다(위 표 참고) → **좌표로 역산한다.**
  경계는 `BERTH_SLOTS` 에서 **자동으로 뽑는다**(`quaySplit()`) — 숫자를 따로 박으면
  슬롯을 늘릴 때 한쪽만 고치게 된다. 실측 경계 quay2 177.5 · quay1 663.
- **여유가 충분하다**: 두 선석 사이 간격이 135px(2안벽) · 228px(1안벽)이고 좌표계 최대
  오차는 23.4px 이다. 도크(실효 경계 27px, 오차와 겹쳐 위험)와 정반대 상황이라
  히스테리시스가 필요 없다.
- ★**선석 id(`quay1`)는 쪼개지 않는다.** "선석이 같으면 안 옮긴다" 판정이 그 id 로 도는데,
  A/B 를 별도 id 로 만들면 A↔B 로 넘어갈 때마다 배를 옮기게 된다. 바뀌는 것은
  **`berth` 에 찍히는 글자 하나**뿐이다(`berthLabelAt()`).
- `loc` 은 안벽 단위로 그대로 둔다(사용자 지시 — 다른 화면이 쓰고 있을 수 있다).
- ★**한 번도 옮긴 적 없는 배는 `berth` 키가 아예 없었다.** 수집이 `moves`·`creates` 에만
  썼기 때문이다(실측: 8292·8300·8209 가 x·y·color 만 갖고 있어 공정관리비서에
  「위치 미확인」으로 떴다). 이제 `skips` 도 채운다 — 자리와 이력은 안 건드린다.

### ★`syncedAt` 은 「바뀐 시각」이 아니라 「**확인한** 시각」이다 (2026-09-05 사용자 지시)
- 칩 글귀를 「위치 갱신」이 아니라 **「위치 확인」**으로 정한 것과 **같은 이유**다. 배가 안
  움직여도 수집은 매번 그 자리에 있음을 확인한다 — 그게 사실이다.
- 옮기는 배에만 찍던 탓에, 6시간마다 확인하고도 공정관리비서에 「(8/30 기준)」이 붙어
  **멀쩡히 확인된 위치가 오래된 것처럼** 보였다(실측 2026-09-05: 23척 중 대부분이
  5~6일째로 표시. 방금 수집이 돌았는데도 그랬다).
- 이제 `skips` 도 매 수집마다 `syncedAt` 을 찍는다. **「언제 옮겼나」는 이 값이 아니라
  `history` 이동 기록에 있다** — 두 질문을 한 필드에 담으려 하지 말 것.
- ★**리스트에 있는 배만 확인으로 친다.** 리스트 밖(`untouched`)·시운전(`sea`)은 그대로 둔다 —
  안 본 배에 "확인했다" 를 찍는 것이야말로 이 레포가 반복해서 당한 조용한 오답이다.
- ★CLAUDE.md 의 "값이 안 바뀌었으면 쓰지 않는다" 는 **이력을 더럽히지 말라**는 뜻이고,
  그건 지켜진다(`history` 에는 안 남긴다). 문서 쓰기는 하루 20척 x 4회 = 80건으로
  무료 한도(20,000/일)의 0.4% 다.
- 검증: `node scripts/test-berth-label.mjs` (슬롯 전수 + 프로덕션 실좌표 8척 + 「id 안 쪼개짐」).

- **A 가 동쪽(오른쪽)**. 안벽 자체는 x 0~1288 까지 이어지지만 1BERTH 동쪽(980~1288)은
  자재지원부·호텔 앞이라 배가 서지 않는다. 지역 버튼 상자에 넣지 말 것.
- 도면에는 A/B 표기가 없다. `안벽` 이라고만 여러 번 적혀 있고 A/B 는 사내 호칭이다.
  그래서 **그림에서 읽을 수 없고, 배가 실제로 붙는 자리로 역산해야 한다.**
- 여기서 한 번 틀렸다: `1안벽` 상자를 x640~1290 으로 잡았더니 1BERTH 동쪽 빈 바다를
  절반이나 담고 정작 **1안벽 B 를 통째로 놓쳤다.** 상자는 배가 붙는 구간만 담는다.

## 🖐 관리자 끌기 — 꾹 누르기 (2026-08-29)

- 두 손가락으로 줌아웃할 때 한 손가락이 배에 닿으면 배가 딸려 나갔다.
  터치는 **600ms 꾹 누른 뒤에만** 끌린다(`DRAG_HOLD_MS`). 마우스·펜은 예전처럼 즉시.
  1초는 여러 척 옮길 때 답답하고 300ms 대는 핀치 시작과 겹친다.
- 누르는 동안 **8px 이상 흔들리면 끌기가 아니라 지도 밀기**로 본다. 마커에 `touch-none`
  이 걸려 있어 브라우저가 대신 밀어주지 않으므로 **그 경우 앱이 직접 스크롤**한다.
  이걸 빼면 배 위에서 시작한 스와이프가 죽은 손짓이 된다.
- 두 번째 손가락이 닿는 순간 잡고 있던 것을 놓는다.
- 잡혔다는 표시(빨간 테두리)는 **React 상태로 두면 안 된다** — 리렌더가 끌던 좌표를
  되돌려 배가 튄다. DOM 에 직접 그린다.

## (참고) 좌표계 확정 절차 — 위 확인으로 대부분 무효
여기서 틀리면 마커가 전부 어긋나고, 그 다음 작업이 전부 헛일이 된다. **지도를 그리기 전에** 확정한다.

1. 3중점검 지도가 **실제 위경도(WGS84)** 를 쓰는가, **자체 픽셀/도면 좌표**를 쓰는가?
   - 네트워크 탭에서 지도 데이터 요청을 보면 대개 바로 드러난다.
   - 값이 `34.7x, 126.3x` 대면 목포 위경도. `0~2000` 대 정수면 픽셀 좌표.
2. **위경도면** → 내 배경지도(SVG 야드 도면)에 얹으려면 **기준점 2~3개**로 변환식을 만든다
   (안벽 끝점처럼 도면과 실제가 확실히 대응하는 지점). 아핀 변환이면 충분하다.
3. **픽셀 좌표면** → 그쪽 지도 이미지 크기 대비 비율로 바꿔서 내 SVG viewBox 에 매핑한다.
4. **검증**: 위치를 아는 호선 2척으로 확인. 하나만 맞추면 회전·스케일 오차를 못 잡는다.

## 🔗 3중점검 → 십로케이션 매칭 파이프라인 (2026-08-29 구축)

로직은 전부 `src/lib/safetyone-match.mjs` 한 곳에 있다(앱·CLI 공용, 의존성 없음).

- **위치의 첫 구간만 본다.** `2도크 > TANK > 1TANK2` 의 `TANK` 뒤는 배 안의 작업
  위치라 야드 좌표와 무관하다.
- **선석이 같으면 안 옮긴다.** 리스트는 구간 안 순서를 주지 않는다. 관리자가 손으로
  다듬은 자리를 수집이 되돌리면 안 된다. (→ 두 번 돌리면 이동 0 — 멱등)
- 선석이 바뀐 배만 그 선석의 빈 슬롯으로(`BERTH_SLOTS`, 실제 계류 위치에서 딴 것).
  만석이면 대기열(y780 빈 바다).
- 시운전·출항·해상·인도 는 야드 밖 — 안 건드린다. 리스트에 없는 호선도 안 건드린다.
  모르는 위치 문자열은 추측하지 않고 "해석 실패" 로 보고한다.
- 반영 경로 셋: ① 앱 관리자 "3중점검 붙여넣기"(미리보기 → 적용, 폰에서 가능)
  ② `node scripts/sync-safetyone.mjs --input <파일>` (자동화용, `--dry` 지원)
  ③ 세이프티원 콘솔에서 `docs/safetyone-capture.js` → JSON 내려받기 (API 후보 목록 포함)
- 이동은 `updateDoc` 부분 갱신이다. `setDoc` 통째 교체는 좌표 밖 필드까지 "건드린"
  걸로 잡혀 touched() 검사형(강화판) 룰에 걸리고, 데이터도 잃는다.
- `berth`(선석 이름)·`loc`(원문)·`syncedAt` 을 ships 문서에 같이 쓴다 — 공정관리비서
  연동용. 룰이 추가 필드를 거부하면 **좌표만으로 폴백**한다(두 룰 모두 에뮬레이터 검증).
## ★2026-08-29 확정 — 세이프티원은 선석 이름을 주지 않는다. 좌표를 준다 ⭐⭐⭐

네 번 골라 네 번 틀린 끝에 스키마를 찍어 보고 알았다. 지도 배 레이어
`/hsemo14/gis/ships` 에는 **선석 이름 필드가 아예 없다.**

| 필드 | 값종류(23척) | 정체 |
|---|---|---|
| `shipno` | 23 | 호선번호 (한 배당 한 줄) |
| `centerTmX`·`centerTmY` | 23 | **전역 실좌표(TM)** ← 이걸 쓴다 |
| `x`·`y` | 22 / 12 | **구조물 안 지역 좌표** — 쓰면 안 된다 |
| `angle`·`rotation` | 3 / 5 | 뱃머리 방향 (우리 각도계와 달라 안 쓴다) |
| `dock` | 3 (9척만) | 도크에 든 배만 |

- **지도가 캔버스인 것도 같은 이유다** — 이름표 없이 그림만 있다. 이름이 든 응답은
  점검이력(`pmo14003r04`)뿐이라 계속 그쪽으로 끌려갔고, 거기엔 야드에 없는 호선까지
  들어 있었다(43개 vs 야드 25척). **이름을 찾는 한 계속 틀린다.**
- `x`·`y` 는 함정이다. 안벽에 붙은 배끼리만 1차식을 맞추면 오차 1~4px 이라 맞는 것처럼
  보이는데, 도크 안 배는 x 가 60~4100 으로 자릿수부터 다르다. 23척을 한 식으로 맞추면
  RMS 215px. **구조물마다 원점이 따로다.**
- `centerTm` → 야드(1380x840) 는 아핀으로 **RMS 12.7px · 최대 23.4px** (실야드 22척).
  두 축 축척 0.46519 / 0.46757 — 0.5% 차이라 회전+균등축척뿐인 강체 변환이다.
  계수는 `src/lib/yard-transform.mjs` 에 박혀 있고, 다시 재려면
  `sync-safetyone.yml` 을 **mode=fit** 으로 돌린다(아무것도 쓰지 않고 계수·잔차만 낸다).

### 여기서 다시 배운 것 — 임계값은 "잰 값의 배수" 가 아니라 "지켜야 할 것" 이 정한다 ⭐
- 잔차 허용치를 실측(12.7px)의 세 배인 40 으로 잡았다. 그런데 그 값이 지켜야 하는 건
  **선석 판정의 여유**였다. 2도크와 1도크 슬롯은 체비셰프 **54px** 뿐이라 실효 경계가
  **27px** 이고, 8300 은 **22.0px** · 8283 은 **23.0px** 만 밀려도 이름이 뒤집힌다 —
  실측 최대 오차 23.4px 와 이미 겹쳐 있었다.
- 전 좌표를 34.4px 밀면 40px 검사를 **통과하면서** 두 척이 반대 도크로 넘어갔다.
  한 번 써지면 다음 수집엔 "그대로" 로 읽혀 **오답이 영구 고착**되고 로그엔 평범한
  이동 한 줄로만 남는다. 이 프로젝트가 네 번 당한 그 "조용한 오답" 이다.
- 고친 방식 둘: 허용치를 경계 아래인 **20** 으로, 그리고 선석 이름에 **히스테리시스**
  (새 선석이 허용치만큼 확실히 가까울 때만 이름을 바꾼다). 진짜 도크 이동은 거리 차가
  훨씬 커서 그대로 잡힌다.
- **판정 로직은 스크립트가 아니라 `yard-transform.mjs` 에 둔다** — 틀리면 배가 엉뚱한
  데로 가므로 파이어스토어 없이 **실제 코드**를 그대로 돌려 검증할 수 있어야 한다.

### ★2026-08-30 정정 — 선수·선미는 **그림에 있다** ⭐⭐⭐

08-29 에 "어디에도 없다" 고 결론 냈는데 **틀렸다.** 사용자가 "선수는 둥글고 선미는
네모나니 파악 가능할 텐데?" 라고 짚어 줘서 다시 재보니 그대로였다.

- **데이터에는 정말 없다.** 배 레이어 22필드 중 각도는 `angle`·`rotation` 둘뿐이고
  값은 `0` 과 `±90` 두 가지 — 축(가로/세로)만 말한다. Supabase 에도 방향 컬럼이 없다.
- **그림에는 있다.** 세이프티원은 배를 **선수는 둥글게, 선미는 네모나게** 그린다.

★내가 왜 틀렸나 — **재는 자리를 잘못 골랐다.**
 처음엔 "끝에서 8% 지점의 폭" 을 쟀다. 130px 짜리 배에서 8% 면 끝에서 11px 인데,
 그쯤이면 둥근 뱃머리도 이미 만폭 가까이 벌어져 있어 차이가 2~3px 로 뭉개진다.
 그 2~3px 을 "노이즈" 로 읽고 **대칭이라고 결론냈다.** 끝에서 **1·2·3·4px** 을 재니
 신호가 이렇게 갈렸다(실측 15척·끝 30개):

 | 끝단 폭 합(만폭 대비) | 정체 |
 |---|---|
 | **1.02 ~ 1.36** | 둥근 끝 = **선수** |
 | **2.57 ~ 4.00** | 네모/뭉툭 = **선미** |

 사이가 1.4↔2.6 으로 비어 있다. 임계값은 `BOW_TIP_MAX 1.8` / `BLUNT_TIP_MIN 2.2`.
 ★교훈: **"차이가 작다" 는 신호가 없다는 뜻이 아니라 잘못된 자리를 재고 있다는 뜻일 수 있다.**
 그리고 도형 안에 **호선번호가 검게 찍혀 있어** 폭을 갉아먹는다 — 구멍을 먼저 메워야 한다.

### ★확대 배수는 재서 고른다 — 3노치는 찍은 값이었다 (2026-08-30)
- run 29 에서 네 구역을 3노치씩 확대해 떴더니 **도형 0~1**. 3노치가 몇 배인지 모르는 채
  고른 값이었다.
- 실측으로 좁혔다(도면을 k 배 늘려 라이브 캔버스 1385x1322 로 오려냄):
  `k=1 통과도형 16 · 1.5 → 11 · 2 → 8 · 3 → 5 · 4 → 4`.
  덩어리 최대가 19071px 로 `MAX_BLOB_FRAC`(36619px) 근처도 못 간다.
  → **덩어리가 커서 걸린 게 아니라 배가 화면 밖으로 나간 것이다.** 남은 배가 서너 척이면
  RANSAC 짝짓기가 애초에 성립하지 않는다.
  ★처음엔 `MAX_BLOB_FRAC` 을 의심했다. 재기 전에 고쳤으면 엉뚱한 상수를 건드릴 뻔했다.
- 그래도 확대는 필요하다. **목적은 선명함이 아니라 호선번호 글자보다 선체를 크게 만드는 것**이다.
  글자는 지도 배율과 무관하게 고정 크기라 기본 배율에서 선체를 두 동강 낸다
  (run 29 z0: 붙임 8척이 **전부** 길이불일치). 글자가 윤곽을 뚫고 나가면 구멍이 아니라
  절단이라 `fillHoles` 로 못 고친다.
- 그래서 가운데에서 **한 노치씩 올리며 각 단계를 한 장씩** 떠서 재 봤다(run 30):

  | | 도형 | 붙임 | 방향 | 축척 |
  |---|---|---|---|---|
  | z0 | 18 | 8 | **0** — 길이비 0.42~0.82, 전부 조각 | 1.125 |
  | **z1** | 11 | 9 | **5** — 길이비 1.07, 끝단 1.0/2.7 | 0.649 |
  | z2 | 4 | 4 | 1 | 0.475 |
  | z3 | 1 | 0 | 0 (덩어리 5개뿐) | — |

  축척 1.125 → 0.649 이므로 **한 노치 ≈ 1.73배**. 위 k 표의 1.5~2 구간과 정확히 겹치고
  "도형 8~11" 예측도 맞았다. **→ 한 노치가 정답. 3노치는 다섯 배 가까이 확대한 셈이었다.**
- 코스는 **네 구역 × 한 노치**. 한 노치면 야드의 3분의 1쯤만 보여 가운데 한 장으로는
  2안벽 서쪽·1BERTH 동쪽이 통째로 빠진다. z0 한 장은 남긴다 — 공짜이고 길이 검사가
  조각을 전부 걸러내므로 틀린 값이 섞일 수 없다(사이트가 바뀌었을 때의 안전망).
- ★**원래 아이디어(네 구역)는 맞았고 배수만 틀렸다.** 안 되면 설계를 갈아엎기 전에
  **어느 축이 틀렸는지 먼저 재라.** 여기선 네 구역이 아니라 노치 수가 틀린 것이었다.
- 도형이 4개 미만이면 `diagnose()` 를 같이 찍는다. run 29 는 "도형 0" 만 남겨서
  왜 0인지 알 수 없었다 — **못 찾았다는 기록에는 왜 못 찾았는지가 같이 있어야 한다.**

### 블록은 양끝이 다 네모다 — 판정하지 않는다 (사용자 지시)
- 도크에 블록 단위로 들어온 호선은 선수·선미가 다 각져 있다. 그런 배는 `bowDeg` 를
  **내지 않고** 예전처럼 선석 관례로 놓는다. 실측 8238·8209 가 정확히 그 꼴이었다
  (양끝 2.7~3.9). 억지로 한쪽을 고르면 절반은 반대로 그린다.

### 파이프라인 (`src/lib/bow-detect.mjs`)
1. 수집이 **지도 캔버스를 직접** 뜯는다(`getImageData`). ★공개 레포이므로 그림은
   절대 파일로 남기지 않는다 — "흰가 아닌가" 1비트 마스크만 넘긴다.
2. 흰 덩어리 → 구멍 메우기 → 주축(PCA) → 양 끝 끝맺음 측정.
3. 도형 ↔ 호선 짝짓기는 RANSAC. ★기준은 **저장 좌표가 아니라 `tmToYard` 로 옮긴
   세이프티원 좌표**다. 저장 좌표는 사람이 다듬어 최대 37px 벌어지는데 이중 계류한
   두 척은 23px 차이라 **두 배가 뒤바뀐다.**
4. 잰 각은 그대로 쓰지 않고 **축 위의 두 값 중 가까운 쪽에 스냅**한다(`headingFromBow`).
   도면이 26.7° 기울어 있어 그대로 쓰면 배가 비스듬히 눕는다.
- 검증: `node scripts/test-bow-detect.mjs` (실제 도면 PNG 를 zlib 로 직접 디코드해서
  돌린다 — 픽스처를 만들지 않는다) · `node scripts/test-bow-plan.mjs`.

### 우선순위: **그림 > 사람 > 선석 관례**
- 그림에서 읽었으면 그게 이긴다. 사람이 정하던 건 애초에 세이프티원이 말을 안 해서였다.
- 못 읽었으면(블록·못 찾음) 사람이 돌려둔 값을 지킨다(`shipHeading`).
- 둘 다 없으면 선석 관례. 관리자 **뱃머리 뒤집기(180°)** 버튼은 그대로 둔다 —
  블록과 수집이 죽었을 때를 위한 것이다.
- 실측: 지금 지도의 12척 중 **8척이 거꾸로** 그려져 있었다(안벽 6척 + 8203·8282).

### ★angle 을 rows 매핑에서 떨어뜨리면 조용히 no-op 이 된다
- `sync-safetyone.mjs` 가 입력 행을 `{hull, tmx, tmy}` 로 다시 만들면서 `angle` 을
  빼먹었다. 그래서 08-29 에 붙인 축 판정이 **한 번도 값을 못 받고** dry 가 "이동 0" 으로
  조용히 통과했다. 새 필드를 쓸 땐 그 매핑을 같이 본다.

### 옮길 때는 슬롯이 아니라 실제 자리에 놓는다
- 슬롯을 쓰던 이유는 "리스트가 구간 안 순서를 안 줘서" 였다. 좌표를 아는 지금 그 제약이
  없다. 다만 **선석이 같으면 안 옮긴다**는 규칙은 그대로 — 손으로 다듬은 자리를 매시간
  12px 씩 흔들 이유가 없다.
- 수집에는 **삭제 권한을 주지 않는다.** 리스트에 없는 호선은 손대지 않는다. 세이프티원이
  잠깐 배를 빠뜨린 것과 배가 진짜로 떠난 것을 기계는 구분하지 못한다 — 오독 한 번이 곧
  데이터 손실이다.
- 지워야 하면 **`remove-hull.yml` 을 사람이 손으로 돌린다**(호선번호를 두 번 받아 다르면
  중단, 지우기 전에 문서를 통째로 로그에 찍고, 이력에도 남긴다). 되돌릴 수 있어야
  지워도 되는 것이고, 로그에 남은 값이면 `restore-positions.yml` 로 그대로 복구된다.
  ★배가 시운전에서 돌아오면 **수집이 알아서 다시 만든다**(`kind: 'create'`) — 이 삭제는
   영구 손실이 아니라 "지금 야드에 없다" 를 지도에 반영하는 것뿐이다.
  ★호선마다 일회성 파일을 만들던 방식(`remove-8285.yml`)은 접었다. 파일이 쌓이면 어느 것이
   최신인지 헷갈리고, **헷갈리면 엉뚱한 배를 지운다.**

### ★2026-08-29 — 수집은 아직 한 번도 돈 적이 없다 (run 16 에서 해소됨)
- (아래는 그날 오전 기록이다. run 16 에서 실제로 반영됐고 크론도 다시 켰다.)
- `sync-safetyone.yml` 실행 이력 **0건**. 시크릿(`SAFETYONE_URL`/`ID`/`PW`)이 없어
  precheck 에서 건너뛰는데, 그게 **success(초록불)로 남아** "수집되고 있구나" 로 읽혔다.
  지도의 배 위치는 여전히 손으로 놓은 좌표다.
- 규칙을 바꿨다: 크론은 건너뛰되 실행 요약에 "설정 안 됨" 을 크게 적고,
  **수동 실행이면 실패시킨다.** 사람이 일부러 돌렸는데 아무 일도 안 하고 초록불이면 거짓말이다.
- 수집 **아래쪽 절반은 검증됐다**(에뮬레이터, 실데이터 25척):
  배가 움직인 리스트 → 이동 2건 정확히 계산·반영, 같은 리스트 재실행 → 이동 0(멱등),
  시운전·리스트 밖 호선은 손 안 댐. 즉 **막힌 건 로그인/스크레이프 한 곳뿐**이다.
- 시크릿을 넣은 뒤 첫 실행은 십중팔구 셀렉터에서 깨진다(사이트를 본 적이 없다).
  실패하면 `out/page.html`·`shot.png` 이 아티팩트로 올라간다 — 그걸 보고 고친다.

- 수집 심장박동은 `meta/safetyone` 문서 하나. 앱이 「위치 확인 N분 전」 칩으로 보여주고
  주기의 1.5배(지금 9시간)를 넘으면 빨갛게 — "몇 시간째 그대로" 와 "수집이 죽음" 을
  구분하기 위한 것. ★임계값은 `SYNC_PERIOD_H` 한 곳에서 내려온다(App.tsx). 크론을 바꾸면
  그 상수도 같이 바꾼다. 주기와 똑같이 잡으면 깃허브 크론이 몇 분 밀릴 때마다 빨개진다
  (스케줄 워크플로는 부하 때 지연·유실된다).
  ★**룰 배포는 2026-08-30 에 풀렸다.** 그 전에는 배포 워크플로가 403 으로 죽어 있었다 —
  `github-deployer@lng-works-482811.iam.gserviceaccount.com` 에 `roles/firebaserules.admin`
  이 없었다(문법 오류가 아니라 권한). 사용자가 권한을 준 뒤 배포 성공 → 수집 로그에서
  `PERMISSION_DENIED` 가 사라지고 심장박동이 처음으로 기록됐다. `scripts/setup-gcp.sh` 의
  역할 목록에도 넣어 뒀다.
  ★**칩 글귀는 「위치 확인 N분 전」이다** (2026-08-30 사용자 지시). `3중점검` 은 세이프티원의
  화면 이름이지 이 칩이 말하는 것이 아니고, `위치 갱신` 도 아니다 — 배가 안 움직여도 이 시각은
  찍히므로 "갱신됐다" 는 사실이 아니다. **확인한 것**이다.
  ★**사내 화면 이름은 화면 글귀에 쓰지 않는다** (2026-08-30 사용자 지시로 확장).
  관리자 버튼도 「**지도 붙여넣기**」이고, 이력에 남는 이유도 하는 일로 적는다 —
  앱 붙여넣기는 `— 지도 붙여넣기`, 자동수집은 칩과 같은 말인 `— 위치 확인`.
  (자동수집은 붙여넣기가 아니므로 같은 글귀를 쓰면 거짓말이 된다.)
  **딱 한 곳만 예외**: 붙여넣기 칸의 안내문 `세이프티원 3중점검 리스트 보기를 복사해…`.
  그건 이름이 아니라 **어느 화면에서 복사하라는 길안내**라, 바꾸면 관리자가 그 화면을 못 찾는다.
  ★**칩은 ⓘ 배너가 아니라 화면 맨 아래 「최근 업데이트」 바의 제목 옆**에 있다. 그 줄은
  넷이 자리를 다툰다(제목 · 칩 · 최근 이력 요약 · 화살표). 좁아질 때 줄어들 것은
  **이력 요약 하나**이므로 제목과 칩에 `shrink-0` 를 준다 — 없으면 칩이 눌려
  「위치 확인 25분 전」의 뒷부분이 잘린다(사용자 보고, 기기 6종 실측으로 확인·수정).
- ★체크인된 `firestore.rules` 는 `is boolean` 오타로 **애초에 배포 불가능한 파일**이었다
  (룰 언어는 `bool`). 고쳐뒀다. 즉 프로덕션 룰은 이 파일과 다른 무언가다 — 배포 전
  프로덕션에서 어떤 쓰기가 되는지는 폴백 로직이 흡수한다.
- 웹 SDK 는 `FIRESTORE_EMULATOR_HOST` 를 무시한다. CLI 가 직접 connectFirestoreEmulator
  를 불러 준다 — 이거 없으면 테스트가 프로덕션을 읽고 쓴다(실제로 한 번 읽었다).

## 📊 접속 집계 — 「하루에 몇 명이, 누가」 (2026-09-05 사용자 지시)

- 관리자 모드 오른쪽 FAB 의 📊 → 「접속 집계」 시트(`VisitsPanel`). 오늘 접속자 수 ·
  최근 14일 일별 · 날짜별 접속자 이름.
- **읽기는 오너 한 사람만.** 판정은 화면이 아니라 **파이어스토어 규칙 `isOwner()`** 이 한다.
  ★이 앱의 관리자 모드는 localStorage 한 줄이라 **누구나 될 수 있다** — 버튼을 숨기는 것은
   예의일 뿐 보안이 아니다. 남이 이 시트를 열어도 `permission-denied` 만 본다.
  ★uid 가 아니라 **확인된 이메일**로 가른다(`email_verified == true` 를 반드시 같이 본다.
   빼면 이메일만 같게 만든 다른 공급자 계정으로 통과한다). 그 주소는 이 레포의 **커밋
   작성자로 이미 공개돼 있어**(git log) 규칙에 적어도 새로 새는 정보가 없고, 이메일은
   비밀번호가 아니라 알아도 못 들어온다. uid 를 쓰면 사람이 한 번 로그인해 값을 알려주는
   왕복이 필요하다.
- **쓰기에는 인증을 요구하지 않는다.** 익명 인증을 켜려면 파이어베이스 콘솔을 사람이 한 번
  눌러야 하고, 그 한 클릭 때문에 기능 전체가 멈춘다. 대신 기기 id 를 브라우저가 만든다.
  ★**위조 가능**하다 — 즉 마음먹으면 접속 수를 부풀릴 수 있다(남의 기록을 읽거나 지우지는
   못한다. 그건 규칙이 막는다). 사내 도구에서 감수할 만한 위험이다. 정확한 사람 수가
   필요해지면 그때 익명 인증으로 올린다.
- **하루에 한 번만 기록한다.** 사용자가 물은 것이 "하루 접속자 수" 이고, 새로고침을 셀 이유가
  없다. 「몇 번 열렸나」는 Cloud Run 요청 로그(`access-stats.yml`)가 이미 답한다 —
  **두 곳이 같은 질문에 다르게 답하지 않게 역할을 갈라 둔다.**
- **이름은 자기 신고다.** 서버가 진위를 확인할 방법이 없다(사내 SSO 가 없다). 그래서 이름에
  아무 권한도 붙이지 않는다 — 사칭해서 얻는 게 0이면 사칭할 이유도 0이다.
  **그 줄을 근거로 사람을 추궁하면 안 된다.**
- 이름 묻는 카드는 **앱을 막지 않는다.** 지도가 뜨고 1.5초 뒤 작은 카드로 뜨고 「나중에」로
  닫힌다. 뷰어에게 이름부터 묻는 전체 화면은 2026-08-30 에 이미 한 번 걷어냈다.
- 규칙은 `hasOnly` 로 필드를 **봉인**하고 시각은 `request.time` 으로 **서버가 찍는다.**
  앱이 나중에 실수로든 편의로든 IP·UA 를 한 줄 붙이면 쓰기가 거부된다 — "안 모으기로 했다"
  는 약속이 지킬 사람 없이도 성립한다. 수정·삭제는 **오너도 못 한다.**
- ★검증: `node scripts/test-visits-rules.mjs` (에뮬레이터 필요). 16단언 —
  인증없음·다른계정·미인증이메일이 **못 읽고** 오너만 읽는 것, 필드 봉인, 서버 시각 강제,
  그리고 **기존 ships·history 쓰기가 그대로 통과**(여기가 막히면 자동수집이 죽는다).
  규칙은 조용히 틀린다 — 조건 하나를 빠뜨려도 에러가 아니라 그냥 통과한다. 그래서 이 파일이 있다.
- ★홈화면 앱(standalone)에서는 구글 **팝업 로그인이 막힌다.** 팝업이 실패하면 리다이렉트로
  물러나고, `getRedirectResult` 를 마운트에서 받는다 — 안 받으면 로그인하고 돌아왔는데도
  그대로 로그아웃으로 보인다.

## 🎨 지도 마커 규칙 (2026-08-29 확정)

세 가지가 전부 **공정관리비서 Supabase 를 읽어** 자동으로 정해진다(`src/lib/vessel-roster.ts`).
십로케이션에서 따로 관리하는 목록은 없다.

- **작업 호선만 진하게.** 공정관리비서 `vessels`(호선 등록부)에 있으면 우리 배 → 그대로.
  없으면 **투명도 0.28** 로 물러난다(실측 25척 중 15척이 대상). z-index 도 낮춰 겹칠 때
  우리 배가 위로 온다.
  ★**조회 실패하면 아무도 안 흐린다**(fail open). 멀쩡한 배가 사라진 것처럼 보이는 쪽이
  덜 정리된 화면보다 훨씬 나쁘다. `working` 이 비면 전부 진하게 = 예전과 같은 화면.

### ★Supabase REST 는 1000행에서 자른다 ⭐⭐⭐ (2026-08-29 사고)
- 처음엔 `vessel_schedules` 에 한 줄이라도 있으면 우리 배로 봤다. 그런데 그 테이블은
  **호선당 144줄씩 1678행**이고, PostgREST 는 응답을 **1000행에서 말없이 자른다**
  (`Content-Range: 0-999/*`, 에러 아님·200 OK). 잘린 1000행에는 물리 순서상 앞쪽
  7척(8198~8207)만 들어와서 **8208·8254·8262·8263·8292·8300 이 "공정 없는 배" 로
  오인돼 흐려졌다.** 정작 우리 배가 사라진 것이다.
- 재현: 옛 코드 = 진하게 4척 / 새 코드 = 진하게 10척 (에뮬레이터 실측).
- **행 수가 호선 수에 비례하지 않는 테이블을 명부로 쓰지 말 것.** `vessels` 는
  호선당 한 줄(15행)이라 상한에 닿을 수가 없다. 요청도 두 번에서 한 번으로 줄었다.
- 그래도 **`length >= 1000` 이면 명부를 못 믿는다 → fail open**. 이 가드가 있었으면
  이번 사고는 화면에 안 나왔다.
- 진단은 추측이 아니라 **Supabase 로그**로 잡았다(`query_logs`, `edge_logs` 의
  `response.headers.content_range`). 다음에도 여기부터 본다.
- **DF 호선은 초록.** `vessels` 의 `containment`·`vessel_name` 에 `DF` 가 있으면 초록,
  아니면 노랑(대부분 LNGC). 보조로 세이프티원 선종 문자열(`15500 CNTR(LNG DF)`)도 본다.
  **관리자 수동 색 선택은 없앴다** — 규칙이 정하므로 손댈 이유가 없다.
- **세로로 선 호선번호는 눕히지 말고 한 자씩 쌓는다** (2026-08-29 사용자 지시).
  `8300` 이면 `8 / 3 / 0 / 0` — 글자는 전부 똑바로 선다.
  - 선체가 화면에서 **가로로 누우면** 글자도 눕혀 한 줄(라벨 회전 ∓90 → 화면각 0).
  - 선체가 **세로로 서면** 선체 각도를 그대로 되돌려(라벨 회전 `-a`) 화면각 0 으로 만들고
    글자를 한 자씩 세로로 쌓는다. 폭 26px 안에 18px, 길이 130px 안에 88px 로 들어간다
    (`leading-[0.85]`, 실측).
  - ★한 번 잘못 갔던 길: 세로일 때 `rotate(90deg)` 로 **글자를 눕혀** 위→아래로 읽게 했다.
    "첫 숫자가 위" 라는 요구를 회전으로 푼 것인데, 원하는 건 회전이 아니라 **쌓기**였다.
  - 검증: 회전 0/90/270 × 배 25척 전부 라벨 화면각 0, 선체 밖으로 안 넘침.

## 🔐 관리자 모드는 **열 때가 아니라 누를 때** 들어간다 (2026-08-30 사용자 지시)

- 예전에는 `localStorage.isAdmin` 이 있으면 앱을 여는 순간 곧바로 관리자 모드였다.
  한 번 관리자였던 기기는 새로고침을 하든 앱을 죽였다 켜든 **계속 관리자로 들어왔고**,
  지도를 보려다 배를 옮기게 된다.
- 이제 **여는 순간은 언제나 뷰어**다. 저장된 관리자 표시는 **권한만** 준다
  (`isAdminUrl` → 정보 배너에 「관리자로 전환」 버튼이 보인다). `?admin=true` 도 같다 —
  예외를 두지 않는다. 예외를 하나 두면 그 경로로 다시 새는 것이 이 프로젝트의 반복된 패턴이다.
  트레이드오프: 관리자는 작업할 때마다 **한 번 더 눌러야 한다.** 사용자가 그쪽을 택했다.
- 관리자로 들어가는 길은 **`enterAdmin()` 한 곳**이다(전환 버튼 · 제목 5번 탭 둘 다 이걸 부른다).
  세 군데에 같은 코드를 복사해 두면 한 군데만 고치게 된다 — 실제로 그랬다.
- **이름 입력 화면을 열 때 띄우지 말 것.** 뷰어로 볼 사람에게 이름부터 묻는 셈이었다.
  이제 관리자로 전환할 때만 뜨고, **「취소하고 뷰어로 보기」** 가 있다 — 이건 더 이상
  앱 진입 관문이 아니라 전환 도중의 대화상자라, 물러날 길이 없으면 새로고침밖에 답이 없다.

## 🔗 공정관리비서(LNG 앱) 연동

- 십로케이션에 `?hull=8300` 딥링크가 있다: 그 배로 날아가 파랗게 깜빡인다.
  LNG 앱 쪽은 Supabase 라 백엔드 공유가 아니라 **읽기 연동**이다: 십로케이션의
  ships 컬렉션은 공개 읽기이므로 LNG 앱이 Firestore REST 로 `berth` 필드만 읽어
  호선 옆에 위치 배지를 달고, 배지 클릭 → 딥링크로 연다.
- **역방향은 앱 진입이 아니라 카드 안 표시다(2026-08-29 사용자 지시).** 뷰어에서 배를
  탭하면 카드가 그 호선의 오늘을 공정관리 Supabase 에서 직접 읽어 보여준다
  (`src/lib/vessel-plan.ts`). 노출 규칙은 **"공정기준 -5일"**: 오늘 걸친 공정 +
  시작 D-5 이내 공정(vessel_schedules).
- **할일탭의 `업무`·`준비` 를 카드에 올린다 (2026-08-30). 그 전에 두 번 틀렸다.**
  ★사용자가 말한 「할일」은 공정관리비서 **사이드바의 `할일` 탭**이다(대시보드 바로 아래).
   `업무` 탭의 `할일 / 진행 / 완료` 칸이 **아니다.** 그 둘을 헷갈려 `work_tasks.status=todo`
   를 올렸다가 통째로 되돌렸다(#72 → #73).
  ★**내가 왜 틀렸나 — 앱 코드를 안 보고 DB 만 보고 추측했다.** 그때는 LNG 레포가 세션에
   안 붙어 있었는데, **붙일 수 있는지 시도조차 안 했다.** `list_repos` 에 안 보이면 끝이
   아니다 — `search_repositories` 로 찾고 `add_repo` 로 붙이면 된다(실제로 그렇게 붙였다).
   **원본이 있는데 추측하지 말 것.** 레포: `iris8ooooo/lng-pump-tower-process-manager`.

### 할일탭의 진짜 정의 — `src/lib/todo-feed.ts` 의 `buildTodoFeed` (실제로 읽음)
  날짜가 있는 것만 모아 날짜순으로 세운다. 미래는 **오늘+14일**까지, 지연은 항상 포함.

  | kind | 출처 | 조건 |
  |---|---|---|
  | `process` 공정 | `vessel_schedules` | `applicable≠false` · 계획일 있음 · `actual_start_date` 없음 · `status≠completed` · 계획일 ≤ +14d |
  | `prep` 준비 | `prep_rules` × 공정 | 트리거 = 계획일 − `lead_days`. `prep_checks` 완료분 제외. **DF호선 제외**(`activity_order > RULEBOOK.length(38)` 인 호선) |
  | `material-request` 자재통보 | `material_items` | `status=pending` + 파생 통보마감 |
  | `material-eta` 자재지연 | `material_items` | 필요일·도착일 지났는데 미입고 |
  | `task` 업무 | `work_tasks` | `status≠done` **그리고 마감일이 있음** · 마감일 ≤ +14d |

  - ★**업무는 `todo` 만이 아니다.** `진행`도 포함하되 **마감일 없으면 아예 안 올라간다.**
    내가 넣었던 `status=todo`(마감일 무관)는 정확히 업무탭 칸이었다.
  - ★**실측(2026-08-30) — 지금 넣어도 화면에 거의 안 뜬다.**
    마감일 있는 미완료 업무가 **DB 전체에 1건**이고 그마저 호선이 없다 → 어느 배에도 안 뜬다.
    실제로 채워지는 건 **`준비`** 뿐이다(8206 17건 · 8207 2 · 8254 2 · 8262 1).
  - ★**자재는 그대로 옮길 수 없다.** 파생 로직이 LNG 앱 `materials.ts` **843줄**이고,
    복사하면 두 앱이 조용히 갈라진다. 서버에 계산해 주는 함수·뷰도 없다(확인함).
    하려면 공용 뷰/RPC 를 DB 에 만들어 둘이 같이 쓰는 쪽이 맞다 — 사용자 프로덕션 DB 를
    건드리는 일이라 반드시 먼저 확인받을 것.
- **공정은 테이블이 아니라 뷰 `vessel_schedule_rows` 를 읽는다 (2026-09-02).**
  한 공정에 묶여 있던 검사가 다른 날 따로 잡히는 경우가 생겼고(부모 행의 `sub_items` jsonb),
  공정관리 쪽이 그걸 별도 행으로 펼친 읽기전용 뷰를 만들어 뒀다. 실측: 부모 행 1678건
  (= 테이블 행 수와 일치) + 세부 4건. anon SELECT 열려 있고 `security_invoker=true` 라
  밑 테이블 RLS 를 그대로 탄다.
  - `sub_key === null` → 예전 그대로의 공정 행. `sub_key` 있음 → 세부 검사 행
    (`activity_order` = 부모 + 0.5, `duration_days` 1, `tank_no`·`applicable` 은 부모 것, `id` 음수).
  - ★**`tw_85`·`final` 같은 종류 값을 코드에 박지 않는다.** 새 종류가 생길 때마다 십로케이션을
    고쳐야 하기 때문이다. **`sub_key` 가 있냐 없냐만** 본다.
  - ★**부모 밑에 붙여 보여주지 않는다.** 카드는 표가 아니라 **날짜순 D-day 피드**다. 세부 검사는
    정의상 부모와 다른 날이고, 부모가 5일 창 밖이면 아예 안 보인다 — 그때 `activity_order` 로
    정렬하면 고아가 된다. 그래서 날짜순을 지킨다.
  - ★★**표기는 `└` 가 아니라 `[19 세부]` 다** (2026-09-03 사용자 지시). 처음에 `└` 들여쓰기를
    썼는데 **`└` 는 "바로 위 줄에 딸린 것" 이라는 뜻**이다. 날짜순 피드에서는 부모가 바로 위에
    없고(다른 날짜라 멀리 떨어지거나 창 밖이라 아예 없다) 그러면 **기호가 거짓말을 한다.**
    번호를 적으면 어느 공정의 세부인지가 **줄 하나로 닫힌다.**
    `3·4탱크 [19 세부] 8.5단 SPRAY PIPE T/W 9/4` — 탱크 → 부모번호 → 이름 순.
    부모 번호는 `floor(activity_order)`(19.5 → 19). **`└` 는 공정 번호순으로 정렬된 표에서만
    맞다** — 십로케이션에는 그런 표가 없다.
    ★교훈: **기호는 배치를 전제한다.** 들여쓰기·`└`·`↳` 는 전부 "위에 부모가 있다" 를 가정하므로,
    정렬 기준이 부모-자식 순서가 아니면 쓰면 안 된다.
  - ★★**뷰로 바꾸면서 조용히 깨질 자리가 둘 생겼다. 둘 다 실제로 깨지기 전에 잡았고,
    회귀 테스트(`scripts/test-vessel-plan.mjs`)가 옛 코드에서 빨간불 4건을 낸다:**
    1. **`activity_order` 가 integer → numeric 으로 바뀐다**(실측: 테이블 integer, 뷰 numeric).
       PostgREST 가 numeric 을 문자열로 내보내면 `v.order === rule.activity_order`(prep_rules 는
       integer)가 **전부 false** 가 되어 **준비가 통째로 사라진다.** 에러가 아니라 빈 목록이라
       아무도 모른다. 프록시가 supabase.co 를 막아 실서버로 확인할 수 없으므로
       **읽는 자리에서 `Number()` 로 못박았다** — 어느 쪽이든 안전하다.
    2. **DF 판정(`max activity_order > 36`)에 세부 행이 섞이면** 표준 호선의 36번 공정에 세부가
       붙는 순간 36.5 > 36 이 되어 **DF 로 오인**되고 준비가 통째로 사라진다. 지금 세부는
       19.5·1.5 뿐이라 안 걸렸지만 시간문제였다. **부모 행만 보고 판정한다.**
    ★교훈: **테이블을 뷰로 바꾸는 건 자료형을 바꾸는 일이다.** 칼럼 이름이 같다고 같은 값이 아니다.
  - 준비 계산(`byTank`)에서도 세부 행을 뺀다 — 준비 룰은 **부모 공정 번호**에 걸린다.
  - **지금 카드에 올린 것은 `업무`·`준비` 둘**이다. 공정·할일 **둘 다 오늘부터 5일**까지만
    본다(2026-08-30 사용자 지시 — 할일탭 원본은 14일이지만 그러면 준비만으로 카드가 꽉 찬다).
    ★임계값은 `HORIZON_DAYS` **한 곳**에서 내려온다. 두 군데 두면 한쪽만 고치게 된다.
  - **탱크 표기는 `tankPrefix()` 한 곳에서 만든다** — 공정·준비가 같은 모양을 쓴다
    (`1·2탱크 PUMP TOWER 탑재` / `1·2탱크 본딩샵 패싱홀 불출`, 2026-08-30 사용자 지시).
    각자 따로 만들면 한쪽만 고치게 된다 — 실제로 공정 `T1·T2 이름` / 준비 `이름 (1·2탱크)` 로
    갈라져 있었다.
    ★**이름 뒤가 아니라 앞에 붙인다.** 뒤에 뒀더니 공정명이 길 때 `truncate` 에 잘려
     탱크번호가 통째로 안 보였다(사용자 보고). 제일 먼저 봐야 할 값이라 앞에 둔다.
  - **같은 준비가 같은 날 걸리면 한 줄로 묶는다** — `본딩샵 패싱홀 불출 (1·2탱크)`
    (2026-08-30 사용자 지시). 묶는 기준은 (룰, 준비 마감일)이다. 마감일 = 공정일 − lead_days
    이고 lead 는 룰마다 고정이라 같은 마감일이면 겨냥하는 공정일도 자동으로 같다.
    ★탱크가 안 붙어 있어도 그대로 나열한다(실측 `본딩샵 맨홀 불출` = 1·3탱크). `1~3` 처럼
    범위로 줄이면 빠진 2탱크가 포함된 것처럼 보인다.
    실측 효과(8206): 준비 17줄 → 묶어서 11줄 → 5일로 좁혀 **5줄**. 카드 전체 20줄 → 8줄.
  - 검증은 **SQL 로 정답을 따로 뽑아 대조**했다. 8206 의 준비 17건이 제목·탱크·트리거일까지
    한 건도 안 틀리고 일치. `vessel-plan.ts` 를 실제로 돌려 비교한 것이지 눈으로 맞춘 게 아니다.
    (대조용 SQL 은 커밋 메시지에 있다. 오늘 날짜에 의존해서 레포에 넣지 않았다.)
- ★**`in.("a","b")` 같은 문법을 넣지 말 것.** 이 개발 환경은 프록시가 supabase.co 를
  막아 REST 문법을 실서버로 확인할 방법이 없고, PostgREST 는 문법이 틀려도 에러가 아니라
  **빈 목록**으로 조용히 돌아온다. 이미 프로덕션에서 도는 `eq.` 만 쓴다.
  **확인할 수 없는 문법은 쓰지 않는다** — 이 프로젝트가 반복해서 당한 "조용한 오답" 이다.
- **카드는 고정폭이 아니다** — `w-[min(92vw,380px)]` 이라 폰마다 비율이 맞춰지고
  태블릿·PC 에서 380px 에서 멈춘다(실측 폴드커버 316 · 갤S20 331 · SE 345 · 14 359 · iPad 380).
  ★그래서 **긴 이름이 잘리는 건 폭 탓이 아니라 `truncate`(한 줄 자르기) 탓**이다. 폭을 늘려도
  안 고쳐진다. 공정명은 최대 96자(평균 30) · 업무 제목 60자 · 준비 제목 24자라, 공정·준비·업무
  **셋 다 줄바꿈으로 전부 펼친다**(2026-08-30 사용자 지시). 96자는 3줄이 된다.
  ★펼칠 때 D-day 를 인라인으로 두면 둘째 줄이 그 밑으로 파고든다 — 세 종류 모두 flex 로
  **D-day 칸을 따로** 두어 글자가 한 칸에 정렬되게 한다.
- ★**호선 카드가 길어지면 오른쪽 조석·바람 FAB 이 글자를 덮는다**(실측 iPhone14: FAB
  y394~450, 카드 y266~710). 그래서 카드가 떠 있는 동안 FAB 열을 위로(`top-20`) 비킨다.
  닫으면 제자리. 목록 높이 45vh 는 아이폰SE(667)에서도 위 ⓘ 버튼을 안 덮는 최대치다. anon publishable 키는 공개용이고 anon SELECT 정책 확인함. LNG 앱의 `?vessel=` 딥링크 처리는 살아 있다(현재 미사용).
  LNG→십로케이션 방향(위치 배지 → ?hull=)은 그대로 산다.
- LNG 레포 푸시는 된다(브랜치 푸시 + MCP 로 PR/머지 — #49 로 확인). "푸시 불가" 경고는 틀렸었다.

## 수집 시 지킬 것
- 갱신이 2~3시간 주기이므로 **폴링도 그 주기에 맞춘다**(예: 30~60분). 1분마다 긁는 건
  얻는 것 없이 사내 서버만 때린다.
- 값이 안 바뀌었으면 **쓰지 않는다**(빈 업데이트로 이력을 더럽히지 말 것).
- 사이트 개편으로 셀렉터/응답이 바뀌는 건 **예정된 일**이다. 파싱 실패 시 조용히 넘어가지 말고
  **마지막 성공 시각**을 화면에 남긴다 — "3시간째 그대로" 인지 "수집이 죽은" 건지 구분돼야 한다.
- 로그인 세션·토큰은 코드에 박지 말고 환경변수로.

---

# 🎨 클로드디자인에 줄 배경지도 프롬프트

> 2026-08-28 확정본 (v2 — 위 "흐림 회귀 방지 인바리언트" 반영).
> 아래 코드블록을 **그대로** 붙여넣는다.
>
> **같이 줄 파일 3개**
> 1. `docs/reference/safetyone-map.png` — 세이프티원 캔버스 원본.
>    파일로 뽑으면 기울어져 나오지만 **사이트 화면에서는 수평이다.**
>    야드가 비스듬한 게 아니라 내려받을 때만 그렇게 나온다.
>    **가공본(회전·크롭한 것)을 주지 말 것** — 리샘플링돼서 흐리다.
> 2. 지금 앱 지도 렌더 — 크기·프레이밍 기준.
> 3. 이 프롬프트.

```
HD현대삼호 조선소 야드 배치도를 SVG 한 장으로 그려줘.
호선(배) 위치 마커를 그 위에 얹을 배경 지도다. 첨부 원본을 보고 벡터로 옮기는 작업이야.

================================================================
■ 입력 파일
================================================================
- safetyone-map.png
  사내 시스템(세이프티원) 3중점검 화면의 야드 도면.

  ★ 기울기에 대해 — 오해하지 말 것
    이 야드는 비스듬하지 않다. 세이프티원 사이트에서는 지도가 정확히 수평으로
    보인다. 첨부한 PNG 가 기울어져 있는 건 canvas 를 파일로 내려받는 과정에서만
    생긴 것이다(화면에서는 회전을 걸어 수평으로 표시하고 있었다).
    즉 기울기는 파일의 사정이지 야드의 모양이 아니다.

    그러니 결과물은 반드시 수평이어야 한다. 첨부 PNG 를 트레이싱하기 전에
    반시계 방향으로 약 23도 돌려 수평으로 맞춰라(23도는 실측한 보정값이다).
    각도를 새로 판단하려 하지 말고, 수평이 됐는지는 current-map.png 와
    비교해서 확인할 것. 그게 사이트에서 보이는 그 방향이다.
    (가로로 길고 바다가 아래로 오는 방향이 맞다)

  참고: 이 원본은 이미 살짝 흐리다. 캔버스 실픽셀 1793x1560 을 화면에서
  2210x2126 으로 늘려 그리고 있었기 때문이다. 흐린 부분은 추측하지 말고
  "안 보임" 으로 두고 알려줘.

- current-map.png
  지금 앱에 들어가 있는 버전. 최종 결과물의 크기·방향·프레이밍은 이것과 같아야 한다.
  단 내용(도형·글자)은 원본을 따를 것. 지금 버전은 틀린 데가 많다.

================================================================
■ 절대 바꾸지 말 것 — 크기
================================================================
- viewBox="0 0 2400 1450" 정확히 이 값. 가로세로 비율도, 숫자도 건드리지 말 것.
- 야드가 이 좌표계를 채우는 위치·크기도 current-map.png 와 동일하게.
- 앱이 이 좌표계 위에 배 마커를 얹는다. 1px 만 어긋나도 저장된 배 위치가 전부 틀어진다.
- 좌표계 설명을 SVG 주석으로 남겨줘: 원점이 어디고, 1 단위가 실제 몇 m 인지.
  원본에서 축척을 못 읽으면 "축척 미상" 이라고 적을 것. 지어내지 말 것.

================================================================
■ 반드시 지울 것 — 배
================================================================
원본에는 그날그날의 배가 같이 그려져 있다. 지도가 아니라 데이터다. 전부 빼야 한다.
앱이 실시간 위치를 직접 그리기 때문에, 배경에 배가 남아 있으면 배가 두 번 보인다.

지울 대상 = 흰색 길쭉한 타원/모서리둥근 도형 안에 "네자리숫자 (선종)" 글자가 든 것.
  8206 (174K LNG) / 8282 (300K VLCC) / 8322 (13000 CNTR(METH. DF)) /
  8314 (8700 CNTR) / 8248 (88K VLAC(LPG DF)) / 8292 (8400 CNTR(LNG DF)) /
  8321 (157K COT) / 8300 (15500 CNTR(LNG DF)) / 8209 / 8238 / 8313 ...
안벽 옆, 도크 안, 돌핀 양옆, 바다 위 — 위치 가리지 말고 전부. 목록은 예시일 뿐이고
같은 형태면 다 배다. 초록색 점 같은 상태 표시도 데이터니 같이 제거.

지운 자리는 그 배가 놓여 있던 바탕색으로 메울 것.
  도크 안의 배 → 도크 바닥색 / 바다 위·안벽 옆의 배 → 바다색

★ 배와 구조물을 한 덩어리로 합치지 말 것.
  이전 시도가 여기서 실패했다. 배 타원 안쪽 색과 도크·돌핀 바닥색이 비슷해서
  같은 도형으로 뭉쳐졌고, 결과적으로 도크와 돌핀이 구분이 안 됐다.
  배는 배, 도크는 도크, 돌핀은 돌핀으로 따로 잡을 것.

================================================================
■ 반드시 남길 것 — 배가 대는 자리
================================================================
배는 지우되 구조물은 남긴다. 이게 없으면 앱이 배를 놓을 자리가 사라진다.
- 1도크 / 2도크 : 세로로 긴 도크 외곽선과 바닥
- 1돌핀 / 2돌핀 : 안벽에서 바다로 뻗은 잔교
- 안벽 : 육지와 바다의 경계선
- 플로팅 도크 자리

================================================================
■ 색
================================================================
- 아래는 서로 뚜렷이 달라야 한다. 지금 버전은 도크와 돌핀이 같은 색이라 구분이 안 된다.
    육지/도로 크림색 / 구역 블록 베이지 / 도크 진회색 /
    돌핀·잔교 도크와 다른 회색 / 안벽 경계 뚜렷한 선 /
    작업구역 연파랑 + 빨강 점선(원본대로) / 바다 #9fc9e4
- 전체적으로 저채도로. 이 위에 얹힐 호선 마커가 튀어 보여야 한다.
  배경이 화려하면 정작 배를 못 찾는다.
- 그라디언트·텍스처·그림자 쓰지 말 것. 확대하면 지저분해지고 파일만 커진다.
- 색을 하드코딩하지 말고 CSS 변수나 currentColor 로. 라이트/다크 둘 다 볼 수 있게.

================================================================
■ 글자
================================================================
- 전부 SVG <text>. 이미지에 구워 넣지 말 것 — 그 순간 래스터가 되어 확대 시 뭉개진다.
- 원본에 적힌 글자를 그대로. 원본에 없으면 넣지 말 것. 지어내지 말 것.
- 글자는 반드시 해당 도형 위에 정확히. 지금 버전은 옆으로 밀린 게 여러 개 있다.
  글자 좌표를 따로 두지 말고 도형의 무게중심에 붙이면 밀릴 수가 없다.
- 필요한 것: 구역 코드(R901~R905, T1~T4, R1~R7, P2~P6, M1/M2, J0,
  B1~B8, G1/G5, Y1~Y3, S02/S3, Q2, A2~A4)와 공장 이름.
- "인도" 라벨은 너무 많아 지저분하다. 최하위 줌 레이어로 내리거나 빼도 된다.
- Bay 번호(72Bay, 54Bay 등)는 중요하지 않다. 빼도 된다.
- 굵은 라벨이 안벽·도크·돌핀 위를 덮지 않게 할 것. 거기가 마커 자리다.

★ 줌 단계별 라벨 레이어로 나눌 것
  축소하면 글자가 뭉친다. 이때 글자를 줄이면 읽을 수 없어진다. 줄이지 말고 숨긴다.
  아래 3단으로 <g> 를 나누고 각각 id 를 달아줘. 앱이 줌 배율에 따라 켜고 끈다.
    id="labels-major" : 선석 이름(안벽·도크·돌핀·플로팅·1BERTH), 대형 공장. 항상 표시.
    id="labels-zone"  : 구역 코드, 공장 이름. 중간 줌부터.
    id="labels-minor" : 인도, Bay 번호, 소형 시설. 많이 확대해야 표시.
  labels-major 는 가장 축소한 상태(약 0.3배)에서도 읽혀야 하니 충분히 크게.

================================================================
■ 선석에 id 달기
================================================================
앱에 "1안벽으로 이동" 같은 버튼이 있다. 좌표를 코드에 박지 않고 SVG 에서 읽어가게
아래 자리에 id 를 달아줘.
  berth-quay1a / berth-quay1b / berth-quay2a / berth-quay2b
  berth-dock1 / berth-dock2 / berth-dolphin1 / berth-dolphin2
  berth-floating / berth-1berth
(1안벽/2안벽/A/B 는 원본 도면에 없다. 사내에서 보기 편하려고 붙인 이름이고
 A 가 동쪽(오른쪽)이다. 도면에는 그냥 "안벽" 이라고만 적혀 있다.)
그 외 건물·구역에도 의미 있는 id 를 달아줄 것.

================================================================
■ 구조와 출력
================================================================
- 단일 SVG 파일 하나. <image> 태그나 래스터 이미지 넣지 말 것.
- 레이어를 <g> 로 분리: 바다 / 육지·도로 / 구역 / 건물 / 작업구역 / 선석 / 라벨 3단.
- 선에는 vector-effect="non-scaling-stroke". 없으면 확대할 때 선이 같이 굵어져 뭉툭해진다.
- 확대는 앱이 viewBox 를 바꿔서 한다. 그러니 루트나 그룹에 transform 걸지 말 것.
- GPU 힌트 절대 넣지 말 것: will-change, transform: translate3d, backface-visibility,
  filter, drop-shadow. 이걸 넣으면 GPU 레이어로 승격돼 그 위 글자가 전부 흐려진다.
  (이 앱과 같은 계열 앱에서 이것 때문에 6커밋을 날린 적 있다)
- :hover 에 transform 쓰지 말 것.
- 외부 폰트 쓰지 말고 system-ui.
- 파일 200KB 이하.

================================================================
■ 확인
================================================================
- 결과물이 수평인지 확인. current-map.png 와 같은 방향이어야 한다.
- 원본 이미지와 결과물을 겹쳐서 안벽선·도크·돌핀·건물 위치가 어긋나지 않는지 확인.
- 배가 하나도 남지 않았는지 확인. 네자리숫자로 훑어볼 것.
- 도크와 돌핀이 색으로 구분되는지 확인.
- 0.3배로 축소했을 때 labels-major 가 읽히는지 확인.
- 원본이 흐려서 못 읽은 글자·도형이 있으면 목록으로 알려줘. 추측해서 채우지 말 것.
```

## 받은 SVG 를 앱에 넣을 때

- `viewBox` 가 `0 0 2400 1450` 인지 먼저 확인. 다르면 되돌려 받는다 —
  여기서 타협하면 저장된 배 좌표가 전부 어긋난다.
- **앱의 현재 줌은 `transform: scale()` 이다.** 위 문서(SVG 로 갈 때 지킬 것)대로면
  `viewBox` 방식으로 바꿔야 한다. 안 바꾸면 확대할 때 글자가 뭉개진다. 별건으로 처리.
- 폰 첫 화면 배율이 약 **0.45** 다. 라벨을 줄이지 말고 `labels-*` 레이어를
  줌 배율로 켜고 끈다.
- 라벨 위치가 또 밀리면 글자 좌표를 따로 갖지 말고 **도형의 무게중심**에 붙인다.

## 📐 반응형 / 모바일

- 현장에서 폰으로 본다. **모바일 우선**으로 설계하고 PC 에 동시 적용.
- 임계값을 여러 군데 흩뿌리지 말 것 — **한 곳에서 정의해 내려준다**. (LNG 앱은 1280px 단일
  기준을 쓰다가, 값이 분산돼 "UI 는 모바일인데 조작은 PC" 인 분열 증상을 겪었다.)
- 터치 타겟 최소 **44×44px**. 호선 마커가 그보다 작으면 투명 히트영역을 덧댄다.
- `position: fixed` 자식에 **safe-area-inset 이중 적용 주의** — 부모가 이미
  `padding-top: env(safe-area-inset-top)` 을 갖고 있으면 노치 인셋이 두 번 먹어
  캡슐이 화면에서 한참 떠 보인다.

### 화면 아래에 쌓이는 것들은 높이를 박지 말고 실측해 내려준다 ⭐ (2026-08-29)
- 아래쪽에 넷이 쌓인다: **호선 카드 → 조석/바람 시트 → 지역 버튼 줄 → 최근 업데이트 바**.
- 조석 시트를 `bottom: 6.5rem(104px)` 로 박아뒀는데, 지역 버튼 줄은 하단바 48px 위에
  **70px(작은 화면) / 78px(sm 이상)** 이라 윗변이 118~126px 이다. 그래서 **폰 전 기종에서
  14px, 아이패드 미니에서 22px** 을 파고들었다(13기종 실측).
- 고친 방식: `usePublishedHeight` 로 실제 높이를 재서 `--dock-h` / `--sheet-h` 로 내려주고,
  위에 얹히는 것은 `calc(3rem + var(--dock-h) + …)` 로 그 값을 참조한다. 브레이크포인트마다
  높이가 달라져도 자동으로 따라간다.
- ★`getComputedStyle(el).bottom` 으로 "아래에 붙었나" 를 판정하면 안 된다. 위치가 잡힌
  요소의 top/bottom 은 `auto` 여도 **계산된 px 로 나온다**(실측: `bottom:auto` 인 데스크톱에서
  `"744px"`). 위아래 중 어디에 더 붙었는지(`innerHeight - rect.bottom < rect.top`)로 가른다.
- 인라인 `style={{bottom}}` 을 브레이크포인트에서 무효화하려면 Tailwind 4 의 중요도 접미사
  `md:bottom-auto!` 가 필요하다. 인라인 스타일은 일반 클래스보다 세다.

## 🔋 폰 발열 / 핀치 — 2026-08-29 실측으로 잡은 것

### 배 모션은 마그네틱에 묶여 있었다 ⭐
- 원래 규칙: **마그네틱 위 = 정지, 아니면 항해 모션**(흔들림·뱃머리 파도·항적·스크류).
- 위치를 3중점검에서 가져오기 시작하면서 마그네틱 위에 있는 배가 **한 척도 없어졌다.**
  그래서 25척이 전부 동시에 애니메이션을 돌렸고 폰이 뜨거워졌다.
- **애니메이션 개수는 배 수에 비례한다.** 배가 늘면 발열도 늘어난다. 정박한 배에
  움직임을 넣지 말 것. 지금은 전부 뺐다(관련 keyframes 5종도 삭제).
- 마그네틱 기능 자체는 살아 있다. 다시 쓸 때 `isShipDocked` 가 그대로 동작한다.

### 뷰포트 메타가 앱 핀치와 싸운다 ⭐⭐
- `index.html` 이 `user-scalable=yes, maximum-scale=5.0` 이면 두 손가락에
  **브라우저 페이지 확대와 앱 지도 확대가 동시에** 걸린다. 증상은 "축소가 뚝뚝
  끊기면서 조금씩만 됨".
- iOS 는 `touchmove` 의 `preventDefault` 로 브라우저 확대를 못 막는다.
  **메타에서 `user-scalable=no, maximum-scale=1.0` 으로 꺼야 한다.**
- 앱이 자체 확대를 가진 지도형 화면에서는 이게 맞다.

### ★핀치가 뚝뚝 끊기는 진짜 원인: `touch-action: pan-x pan-y` (2026-08-29)
- 뷰포트 메타(`user-scalable=no`)를 고쳤는데도 안 나았다. 가설이 틀렸던 것이다.
- 실제 원인: 지도 뷰포트에 `touch-pan-x touch-pan-y`(= `touch-action: pan-x pan-y`)가
  걸려 있었다. 이 값은 브라우저에 **"한 손가락 스크롤만 하고 핀치는 없다"** 고 알린다.
  두 손가락이 닿으면 브라우저가 **몇 번의 touchmove 뒤에 `touchcancel` 을 던져**
  제스처를 죽인다. 그래서 "조금 줄었다가 멈추고, 뗐다 다시 하면 또 조금" 이 된다.
  (Firefox bug 964750 / w3c pointerevents #166 / MDN·CSS-Tricks 교차 확인)
- **지도처럼 자기가 팬·줌을 다 하는 요소는 `touch-action: none` 이 정답**이다(MDN 이
  드는 대표 예가 바로 지도다). 대신 **한 손가락 밀기를 앱이 직접 구현**해야 한다 —
  `touchmove` 에서 `scrollLeft/scrollTop` 을 직접 움직인다.
- 두 번째 손가락의 `touchstart` 가 **항상 `touches.length===2` 로 오지는 않는다**
  (실측: 거의 동시에 닿으면 1짜리 하나만 온다). touchstart 에만 기대 기준거리를
  잡으면 그때 핀치가 통째로 죽는다. **`touchmove` 에서도 기준을 잡게** 해둘 것.
- 배율에 **CSS transition 을 걸어두지 말 것.** 매 프레임 DOM 에 직접 쓰는데
  전환이 얹히면 손가락보다 늦게 따라와 그것도 "끊김" 으로 보인다. `isPinching`
  같은 React 상태로 끄려 해도 리렌더가 한 프레임 늦어 못 막는다.

### ★그래도 끊겼다 — 진짜 원인은 **리렌더가 손가락 밑의 노드를 갈아치우는 것** (2026-08-30) ⭐⭐⭐

`touch-action: none` 을 고친 뒤에도 사용자는 "찔끔찔끔만 되다가 많이 축소되면 그때부터
또 된다" 고 했다. **가설이 하나 더 틀렸던 것이다.**

**잰 것** (헤드리스 크로미움, iPhone14 크기, 손가락 300→60px 을 6단계로 실제 디스패치):
- 배율이 **첫 한 프레임(0.96배)만** 움직이고 멈춘다. 브라우저가 배달한 touchmove **1/6**.
  touchcancel 은 오지 않는다 — 조용히 끊긴다.
- 터치 타깃을 찍으니 `<path> isConnected=false`. MutationObserver 에 `childList -2 +2`.
- `applyZoomAt` 의 네 가지 쓰기를 하나씩 빼서 갈랐다:
  `setZoom` 만 빼면 **6/6 · 1.788→0.358(손가락 비율 그대로)**, 나머지(스크롤·크기·transform)를
  빼도 1/6 그대로. → **범인은 React 리렌더 하나.**

**확정된 것**: `dangerouslySetInnerHTML={{ __html: mapSvg }}` 는 JSX 안에서 만들면
**렌더마다 새 객체**다. React 19 는 이 prop 을 **객체 동일성**으로 비교하므로 문자열이
같아도 `innerHTML` 을 통째로 다시 쓴다. 지도 SVG 가 전부 새 노드로 갈리고, 손가락이
얹혀 있던 `<path>` 가 문서에서 뜯겨 나간다. **타깃이 사라진 터치는 브라우저가 더 이상
touchmove 를 배달하지 않는다.** → 핀치가 한 프레임 만에 죽는다.
고치는 법은 **모듈 상수 하나**: `const MAP_HTML = { __html: mapSvg };`

**추론인 것**: "많이 축소하면 그때부터 잘 된다" 는 축소하면 손가락이 SVG 밖(뷰포트
바탕)에 닿을 확률이 높고 그 요소는 안 갈리기 때문으로 본다. 축소 상태에서 손가락을
SVG 위에 정확히 놓고 재 보지는 않았다.

★교훈 셋:
1. **"제스처가 끊긴다" 를 전부 `touch-action` 탓으로 돌리지 말 것.** 그건 08-29 의
   원인이었고, 이번 것은 **DOM 노드가 사라진 것**이다. 원인이 같은 증상으로 두 번 온다.
2. **끊기는 제스처는 이벤트 배달 수부터 센다.** "몇 번 디스패치했고 몇 번 배달됐나" 한
   줄이 가설 다섯 개를 지웠다. 앱 로직을 들여다본 시간이 전부 헛수고였다.
3. **`{{ }}` 로 넘기는 prop 은 매 렌더 새 객체다.** `dangerouslySetInnerHTML`·`style` 처럼
   비싼 prop 은 모듈 상수나 `useMemo` 로 굳힌다.

### ★지역 버튼: 이동 중 누른 것을 **버리지 말고 이어받는다** (2026-08-30)
- "가끔 전체 버튼이 안 먹힌다. 다른 버튼 눌렀다 다시 전체 누르면 된다."
- `flyTo` 가 `if (flyingRef.current) return;` 이었다. 이동 애니메이션이 **620ms** 라
  그 안에 누른 버튼은 **통째로 사라진다.** 실측: 1안벽 누르고 100·300·500ms 뒤 전체 →
  전체 무시. **700ms 뒤면 정상.** 사용자가 "다른 버튼을 한 번 거치면 된다" 고 한 것도
  그 사이에 620ms 가 지나서였다 — 다른 버튼이 뭘 풀어준 게 아니다.
- 고친 방식: 이동에 번호(`flySeqRef`)를 붙이고, 새 이동이 시작되면 번호가 바뀌어
  **이전 rAF 루프가 다음 프레임에 스스로 물러난다.** 회전·핀치도 같은 방식으로 이어받는다.
- ★같이 고쳐야 하는 것 둘:
  1. **이동 중에는 `zoomRef` 가 옛 값이었다.** 배율을 매 프레임 DOM 에만 쓰고 끝에서
     한 번 `setZoom` 했기 때문. 그래서 이동 도중 핀치하면 **이동 전 배율**을 기준으로
     잡아 튀었다. step 안에서 매 프레임 `zoomRef.current = z` 로 둔다.
  2. **이동을 중간에 멈추면 그 자리 배율을 `setZoom` 으로 커밋해야 한다.** 안 하면
     다음 리렌더가 JSX 의 옛 `zoom` 으로 transform 을 다시 써서 지도가 뒤로 튄다.
- ★이 구조(이동 중 입력 무시)는 rAF 가 안 도는 상황(탭 백그라운드)에서 `flyingRef` 가
  영영 `true` 로 남아 **모든 버튼이 죽는** 길도 열어 뒀었다. 이어받기로 그 길도 막혔다.

### 지도 회전은 컨테이너 transform 으로만 (2026-08-29)
- 가로로 긴 야드(1380x840)를 세로 폰에 맞추면 0.25 밖에 안 된다. **90도 세우면
  0.45 로 전체가 다 들어온다.** 0/90/270 세 방향.
- 좌표계는 절대 건드리지 않는다. `translate(...) rotate(θ) scale(z) translate(pad,0)`
  하나로 끝내고, 지도↔화면 변환은 `mapToContent`/`contentToMap`/`screenDeltaToMap`
  한 곳에만 둔다. 스크롤 목표·핀치 기준점·배 끌기가 전부 이걸 거쳐야 한다.
- `marginLeft` 로 여백을 주면 안 된다 — 회전 전 레이아웃이라 세우면 어긋난다.
- **호선번호는 화면이 아니라 선체를 따라간다.** 화면 기준으로 붙잡으면(회전각을
  빼면) 세운 상태에서 가로로 누운 배 위에 글자가 세로로 얹혀 삐져나간다(실측).
  선체 방향 ±90 중 **뒤집히지 않는 쪽**을 고른다(`hullLabelRot`).

### 배포해도 옛 화면이 뜨면 nginx 를 봐라 ⭐
- `location = /index.html` 은 URL 이 정확히 `/index.html` 일 때만 잡는다.
  폰은 `/` 로 들어오고 그건 `location /` 의 try_files 가 처리하므로 **응답에
  Cache-Control 이 아예 안 붙었다.** 홈화면에 설치한 iOS 웹앱이 그걸 물고 있으면
  배포를 해도 몇 시간씩 옛 화면이 뜬다. `location /` 에도 no-cache 를 건다.

### 첫 화면은 '전체' 이고, 방향은 **재서 고른다** ⭐ (2026-08-29)
- 예전 첫 화면은 배가 놓이는 띠(860x590)에 맞췄는데, 화면이 넓으면 그 계산이 상한
  0.9 에 그대로 붙어 **2안벽 하나만 꽉 찬 채로** 시작했다. 어디를 보고 있는지 알 수 없다.
  지금은 `전체` 버튼과 **같은 배율·같은 중심**으로 진입한다.
- `전체` 는 딱 맞추지 않고 **10% 더 당긴다**(사용자 지시). 정확히 맞추면 둘레에 빈 바다만
  남아 정작 배가 작아진다. 상한도 0.9 → 1.0 — 데스크톱에서 0.9 가 그 10% 를 통째로 먹었다.
- **방향은 두 방향을 실제로 재서 큰 쪽을 고른다.** 야드가 가로로 길어(1480x840) 세로 화면에
  눕히면 30% 만 채운다(실측 iPhone14 33% · 갤S20 32% · 폴드커버 27%). 세우면 100%.
  ★"폭 640 미만이면 세운다" 같은 임계값을 두지 말 것 — 아이패드 미니(744 세로)를 놓친다.
  `fullFit(w,h,90) > fullFit(w,h,0)` 한 줄이면 기기 목록을 관리할 필요가 없다.
- ★`Number(localStorage.getItem(k))` 는 키가 없어도 **0** 이다. `YARD_ROTS.includes(0)` 이
  참이라 "0도가 저장돼 있다" 로 읽혀 기본값에 영영 닿지 못했다. raw 를 먼저 null 검사할 것.

### 배는 절대 겹쳐 놓지 않는다 ⭐ (2026-08-29)
- 8265 를 실좌표 (83,575) 에 놓았더니 8206(75,560) 과 8·15px 차이라 완전히 포개졌다.
  **좌표가 틀린 게 아니다** — 세이프티원 기준으로도 둘은 21px 차이이고, 즉 실제로 나란히
  붙어 있는(이중 계류) 배들이다. 마커 폭이 26px 이라 그 간격이면 화면에서 겹칠 수밖에 없다.
- 원인은 "실제 자리에 놓기" 경로가 슬롯 경로에 있던 **자리 점유 검사를 건너뛴 것**.
  실좌표는 **비어 있을 때만** 쓰고, 차 있으면 그 선석의 빈 슬롯(이중 계류 자리 포함)으로,
  그것도 없으면 대기열로 물러난다.

### 겹침은 "번호가 가려지는가" 로 잰다 — 선체로 재면 과하다 ⭐
- 처음엔 `Chebyshev < 22` 한 개짜리 문턱으로 쟀다. 마커는 정사각형이 아니라 **26x130 이고
  회전한다** — 8265 는 세로로 15px 떨어져 그 검사를 통과했는데 번호가 통째로 가려졌고,
  반대로 길이 방향으로는 88px 까지 가려지는데 22px 검사는 그걸 아예 못 본다.
- 그렇다고 **선체 상자(26x130)로 재면 이번엔 과하다.** 1BERTH 의 8209(966,368)와
  8238(966,484)은 116px 떨어져 선체 끝이 14px 스치지만 번호는 멀쩡히 읽힌다. 그걸
  겹침으로 잡으면 **멀쩡한 배를 매시간 옮긴다**(실측: 22척 중 2척이 걸렸다).
- 지켜야 하는 건 선체가 아니라 **번호가 읽히는 것**이다. 그래서 번호 칸
  **18x88** 으로 잰다(`labelsHidden`). 실야드 22척은 한 척도 안 걸리고,
  8265 만 정확히 잡힌다.
- ★그리고 **이미 겹쳐 써진 것은 스스로 못 풀린다.** "선석이 같으면 안 옮긴다" 때문에
  다음 수집마다 "그대로" 로 읽혀 영영 겹친 채 남는다. 그래서 계획 마지막에 최종 자리를
  다시 훑어 겹친 배를 옮긴다 — 그 규칙은 **손으로 다듬은 자리**를 지키려는 것이지
  가려서 안 보이는 자리를 지키려는 게 아니다. 슬롯에 가까운 쪽이 자리를 지키고
  어긋난 쪽이 옮겨진다.

### 지역 버튼 배율은 구역마다 달라야 한다
- 안벽은 가로 620~650 이라 도크와 같은 여백(FIT 0.8)으로 맞추면 0.43 에 착지한다.
  `전체`(0.30)와 차이가 없어 **"버튼을 눌러도 아무 일도 안 일어난 것처럼" 보인다.**
- `YardRegion.fit` 으로 구역별 배수를 준다. 도크·돌핀 0.68 / 안벽 1.15.

### 지도 왼쪽 여백은 viewBox 가 아니라 앱에서 준다 ⭐
- 야드 서쪽 끝은 해안이 아니라 **도면이 잘린 자리**다. 블록이 화면 끝에 딱 붙어,
  오른쪽이 바다로 열려 있는 것과 대비돼 축소하면 한쪽으로 쏠려 보인다.
- `viewBox` 를 왼쪽으로 넓히면 좌표계가 바뀌어 **저장된 호선 좌표가 전부 어긋난다.**
  지도는 그대로 두고 앱이 스크롤 영역만 `YARD_PAD_L` 만큼 넓힌다(래퍼 폭 + 안쪽 marginLeft).
- 여백을 넣으면 **첫 화면 배율 계산에도 같이 넣어야** 한다. 빼면 여백이 화면 밖으로
  밀려나 덧댄 의미가 없다. 하한도 같은 비율로 내린다(0.24 → 0.22).
- 지역 버튼의 하한(`Math.max(0.3, …)`)도 같이 본다. 첫 화면이 0.25 인데 하한이 0.3 이면
  **`전체` 를 눌렀을 때 첫 화면보다 확대돼 왼쪽 끝이 잘린다.**

### 첫 화면 배율 하한은 지도 좌표계를 바꿀 때 같이 고쳐야 한다
- 2400폭 시절 하한 `0.34` 가 1380폭 지도에 남아 있었더니, 화면 폭 계산을 이겨서
  **모든 폰에서 지도가 화면보다 넓게** 떴다. 좌표계를 바꾸면 하한도 같이 본다.

### 기종 테스트는 13개로 돈다
갤S20 360x800 / iPhone SE 375x667 / iPhone14 390x844 / iPhone Max 430x932 /
**Z폴드 커버 344x882 · 376x968** / **Z폴드 펼침 768x1104 · 816x1076** /
iPad mini 744x1133 / iPad Pro 834x1194 / 폰 가로 844x390 / iPad 가로 1194x834 /
데스크톱 1440x900.
- **폴드 커버(344px)가 가장 빡세다.** 여기서 안 넘치면 대개 다 통과한다.
  실제로 지역 버튼이 여기서 53px 넘쳐서 640px 미만은 버튼을 줄였다.
- 검사 항목: 가로 넘침 / 고정 요소끼리 겹침 / 첫 화면 배율.
  스크립트는 세션 스크래치의 `devices.mjs` 형태로 매번 다시 쓴다.

## 📱 홈화면 아이콘·이름 (2026-08-30)

### 아이콘은 `public/icon.svg` 한 장에서 나온다
- 바꾸려면 **그 파일만 갈아 끼우고 `node scripts/make-icons.mjs`.** 끝이다.
  PNG 다섯 장(180 iOS · 192/512 manifest any · 512 maskable · 32 파비콘 폴백)을 굽고,
  `index.html`·`public/manifest.json` 의 경로까지 스크립트가 고쳐 쓰고, 예전 PNG 는 지운다.
  손으로 만질 파일이 없다 — 손으로 만지게 두면 세 곳 중 하나를 빠뜨린다.
- 새 그림의 바탕색이 다르면 `BG` 도 같이 바꾼다. 잊지 않도록 스크립트가 **그림 가장자리
  색을 실제로 재서** BG 와 비교하고, 16(채널당) 넘게 벌어지면 크게 찍는다.
  급하면 `ICON_BG=#rrggbb node scripts/make-icons.mjs`.
- 이 기계의 크로미움 리비전이 레포 playwright 와 다르면
  `CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome node scripts/make-icons.mjs`.

### ★파일 이름에 그림 해시를 박는다 — iOS 웹클립 캐시가 이름으로 문다 ⭐⭐⭐
- `icon-180.e67b3131.png` 처럼 SVG 내용의 sha256 앞 8자리가 이름에 들어간다.
- **같은 이름으로 그림만 갈아 끼우면 지우고 다시 추가해도 옛 아이콘이 되살아난다**
  (Apple 개발자포럼 23984 · discussions 8186768). `?v=2` 같은 쿼리스트링은 잘 안 통하고
  **파일명 자체를 바꾸는 것**이 확실한 우회다.
- 해시라서 사람이 버전을 올릴 필요가 없다 — 그림이 바뀌면 이름이 저절로 바뀌고,
  안 바뀌면 그대로다. 기억에 기대는 규칙은 반드시 잊힌다.
- **아이콘 경로를 새 파일에서 참조하면 `make-icons.mjs` 의 `REFS` 에 같이 추가할 것.**
  거기 없으면 이름이 바뀔 때 그 참조만 남아 404 가 된다(스크립트가 참조 개수를 세서
  1개가 아니면 에러로 멈춘다 — 조용히 넘어가지 않는다).

### ★iOS 는 `apple-touch-icon` 에 SVG 를 못 쓴다 ⭐
- **잰 것**(2026-08-30 pwa-check): 프로덕션의 `apple-touch-icon` 이 `/icon.svg` 를
  가리키고 있었고 PNG 는 **한 장도 없었다**(없는 PNG 가 `200 text/html` 로 나왔다).
- **확정된 것**: apple-touch-icon 은 **래스터만 되고 벡터는 안 된다.** Apple 공식
  (아카이브)이 PNG 형식만 말한다. jpg 도 형식상 받긴 하나 아이콘이 검게 변하는 사례가
  있어 **PNG 가 유일하게 안전한 선택**이다. ("PNG 만 읽는다" 는 조금 과한 표현이다.)
- ★**추론인 것 — 실측이 아니다.** "그래서 아이폰에 페이지 스크린샷이 박힌다" 는
  **아무도 실제 아이폰 홈화면을 본 적이 없다.** 잰 것은 HTTP 응답뿐이다.
  스크린샷 폴백 자체는 널리 보고되지만 Apple 이 문서화한 적이 없고 무조건적이지도 않다.
- 폴백은 한 방에 스크린샷이 아니라 **네 단계**다:
  ① head 의 `apple-touch-icon` → ② 루트 `/apple-touch-icon.png`·`-precomposed.png` 를
  직접 요청 → ③ **manifest icons**(Safari 15.4+) → ④ 스크린샷.
  ①이 **유효하면** manifest 보다 우선한다. 즉 "apple-touch-icon 이 깨졌다" 가 곧
  스크린샷은 아니다 — 그때 이 레포는 ①②③ 이 전부 비어 있었다(manifest 도 SVG 한 장,
  nginx SPA 폴백이 루트 probe 에 200 text/html). 그래서 개연성은 높지만 **확인은 안 됐다.**
- ★교훈: 고친 것이 옳아도 **왜 옳은지를 과장해 적으면 다음 세션이 잘못된 모델로 판단한다.**
  잰 것 · 확정된 것 · 추론한 것을 갈라 적는다.
- iOS 는 투명 픽셀을 **검게** 칠한다. 그래서 생성기가 바탕색으로 꽉 채워 내보낸다
  (모서리 둥근 SVG 를 그대로 쓰면 검은 귀퉁이 + iOS 둥근 마스크가 겹쳐 지저분해진다).

### ★2026-08-30 아이콘 교체(시안 1j) 에서 얻은 것

**핸드오프는 그림만 받고 배관은 우리 것을 쓴다.**
디자인 쪽 번들에 `manifest.json`·`index.head.html`·`rasterize.mjs`(sharp)가 같이 왔는데
그대로 쓰면 오늘 세운 것들이 통째로 되돌아간다: 앱 이름이 **옛 이름**으로,
`id`·`scope` 삭제, 아이콘 파일명에서 **해시 제거**(iOS 웹클립 캐시 우회가 사라진다),
manifest icons 에 **SVG 추가**(`sizes:"any"` SVG 가 192/512 PNG 를 밀어내고 주 아이콘이
된다), 그리고 안 써도 되는 sharp 의존성. **그림 두 장만 받았다.**

**maskable 은 전용 원본이 있으면 그걸 쓴다.**
`public/icon-maskable.svg` 가 있으면 maskable PNG 는 축소 없이 그걸로 굽는다.
전용 원본은 배경이 **풀블리드**(둥근 모서리 없음)이고 콘텐츠만 80% 안에 있어서 안드로이드가
어떤 모양으로 잘라도 배경이 끝까지 이어진다. `icon.svg` 를 80% 로 줄이는 방식은 배경까지
같이 줄어 테두리에 단차가 남는다. ★**해시는 두 원본을 다 넣는다** — maskable 만 고쳤는데
이름이 그대로면 안드로이드가 못 알아챈다.

**BG 는 단색이 아니라 CSS background 값이면 뭐든 된다.**
1j 는 바탕이 위아래 그라디언트(`#e6f0f8`→`#bed6e9`, 양 끝 40 차이)라 **단색으로는 위아래 중
한쪽이 반드시 어긋난다.** 같은 그라디언트를 깔면 네 귀퉁이 차이가 0~1 로 떨어진다.
그래서 검사도 hex 파싱이 아니라 **배경을 실제로 그려 같은 자리 픽셀끼리 비교**한다 —
그래야 그라디언트에서도 잰다.
★이 검사가 첫 실전에서 바로 잡았다: 옛 단색 `#0284c7` 을 그대로 두고 돌렸더니 **188 차이**로
빨간 줄이 떴다. 잊는 걸 막으려고 만든 장치가 실제로 잊은 순간에 작동했다.

**`theme_color`·`background_color` 도 아이콘과 같이 본다.**
`#0284c7` 은 **옛 아이콘에서 딴 색**이라 새 그림에는 없는 색으로 남아 있었다.
앱 루트가 `bg-[#2c3e50]` 이므로 둘 다 `#2c3e50` 로 맞췄다 — 브라우저 크롬·스플래시가
실제 화면과 같아진다. 아이콘을 바꾸면 이 두 값이 고아가 되지 않았는지 반드시 확인한다.

### 설치본 사용자에게 아이콘 변경을 알린다 (`IconUpdateNotice`)
- 아이콘을 바꿔 배포해도 **이미 만들어 둔 것은 저절로 안 바뀐다.** 자동화 가능 범위가 갈린다:
  브라우저 탭 = 자동 / 안드로이드 크롬 = 대체로 자동(24시간 주기) /
  PC 크롬 설치본 = 사용자 승인 필요 / **아이폰 홈화면 = 경로가 아예 없다.**
  마지막 하나 때문에 앱이 직접 말해 주는 것 말고 방법이 없다.
- **버전 상수를 새로 두지 않는다.** `apple-touch-icon` href 에 이미 그림 해시가 박혀 있어
  런타임에 읽는다. 상수를 두면 아이콘 바꿀 때 같이 올리는 걸 잊는다.
- 설치본(standalone)으로 열렸고 `localStorage.iconSeen` 이 지금 해시와 다를 때만 뜬다.
  ★**첫 설치인지 예전 설치인지 가르는 확실한 신호가 없다.** 다른 키가 하나도 없으면
  첫 설치로 보고 조용히 기록만 한다 — 트레이드오프: 예전에 설치했지만 아무 설정도 안
  건드린 사람은 안내를 놓친다. 반대로 하면 **방금 추가한 사람에게 지우라고 말하게 되는데**
  그게 더 나쁜 오답이다.

### ★첫 판은 구조가 틀렸다 — 안내는 그 화면에서 실행 가능해야 한다 ⭐⭐
- 이 카드는 **설치본 안**에서 뜬다. 거기엔 **주소창도 공유 버튼도 없다.** 그런데 첫 판은
  "Safari 로 다시 열어 공유 → 홈 화면에 추가" 라고만 적었다 — **주소를 꺼낼 방법을 주지
  않고 주소가 필요한 일을 시킨 것이다.** 사용자가 따라 할 수 없다고 바로 지적했다.
- 그리고 「북마크 제거」 같은 **메뉴 이름은 iOS 버전마다 다르다**(13~16 은 "북마크 삭제",
  17+ 는 앱처럼 "홈 화면에서 제거"). 단정하면 그 버전이 아닌 사람은 못 찾는다.
  → 이름을 못 박지 말고 "길게 눌러 나오는 메뉴에서 삭제" 로 쓴다.
- 고친 방식: **주소를 직접 건네준다.** `Safari에서 열기`(설치본에서 `target=_blank` 는
  Safari 로 나간다) + `주소 복사`(클립보드) + **눈에 보이는 주소**(복사가 막혀도 길게 눌러
  집는다). 셋 중 하나는 반드시 통한다.
- ★복사하는 주소는 `location.href` 가 아니라 **`origin + '/'`** 다. 지금 주소에
  `?admin=true` 가 붙어 있으면 그대로 복사돼 **관리자 권한이 딸려 나간다.**
- ★`X` 는 기록하지 않고 `다 했습니다` 만 기록한다. 실수로 닫았을 때 영영 못 보면 안 된다.
- 순서도 바꿨다: **새로 추가한 다음 옛것을 지운다.** 먼저 지우라고 하면 지금 보고 있는
  앱을 잃는다.

### ★「홈 화면에 추가」 이름 칸의 옛 이름은 **우리가 못 고친다** (2026-08-30 실측)
- 사용자 보고: 사파리 「홈 화면에 추가」 시트의 이름 칸에 아직 `ship location` 이 뜬다.
- **전 계층을 재 봤고 우리 쪽은 다 맞다:**
  프로덕션 `<title>`·`apple-mobile-web-app-title`·manifest `name`·`short_name` 네 곳 모두
  새 이름(pwa-check) / `document.title` 을 바꾸는 코드 없음 / **서비스워커는 이 레포에
  있었던 적이 없음**(git 이력 확인) / `<title>`·manifest 링크 각각 하나뿐 /
  크롬 CDP `Page.getAppManifest` 가 **오류 0**으로 한글 `name`·`short_name` 을 읽음.
- ★`manifest.json` 이 `application/json` 으로 나가는 것은 **원인이 아니다.** 규격이 권장하는
  건 `application/manifest+json` 이지만 **아무 JSON MIME 이나 받아들인다**(Lighthouse 경고
  수준). 재기 전에 이걸 고치고 "해결했다" 고 할 뻔했다 — 아침의 `MAX_BLOB_FRAC` 과 같은 함정.
- **그래서 결론: 아이폰이 그 주소에 대해 기억해 둔 옛 제목을 이름 칸에 미리 채우는 것이다.**
  `HD현대삼호 SHIP LOCATION` 은 AI Studio 시절 제목이다. 서버를 고쳐도 그 칸은 안 바뀐다.
  ★단 이건 **추론이다** — 아이폰 내부를 본 것이 아니라, 우리 쪽이 전부 맞다는 실측에서
  남은 설명이다.
- **고칠 수 있는 건 그 칸이 입력칸이라는 것뿐이다.** 그래서 안내 카드가 ① 옛 이름이 뜰 수
  있다고 경고하고 ② 고칠 이름을 보여 주고 ③ **「이름 복사」** 버튼을 준다.
- ★이름은 **DOM 에서 읽는다**(`src/lib/app-name.ts` → `apple-mobile-web-app-title`).
  다섯 번째 사본을 만들면 이름 바꿀 때 이것만 빠뜨린다 — 아이콘 해시와 같은 원칙.
- **안드로이드는 이 문제가 없다.** 크롬은 매니페스트 `short_name` 을 쓰고 방문 기록 제목을
  안 쓴다. 그래서 그 경고는 아이폰에만 띄운다. 단 **이미 설치된 안드로이드 앱**은
  `name`·`short_name` 이 보안 민감 필드라 재설치 전까지 옛 이름 그대로다.

### 카톡으로 주소를 뿌릴 때 — 설치는 카톡 안에서 절대 안 된다 (`InstallGuide`)
- **카카오톡 인앱브라우저에는 "홈 화면에 추가" 가 아예 없다.** 안드로이드에서 홈 화면
  바로가기를 만들 수 있는 브라우저는 **크롬·삼성인터넷뿐**이고, 아이폰은 Safari 공유 시트
  뿐이다. 그래서 **카톡 링크만 눌러서는 설치가 절대 안 뜬다** — 먼저 진짜 브라우저로
  넘어가야 한다. 네이버·인스타·라인 인앱브라우저도 같다.
- 플랫폼별로 할 수 있는 것이 다르다:

  | | 할 수 있는 것 |
  |---|---|
  | 카톡 등 인앱브라우저 | 설치 불가. 안드로이드는 `intent://…;package=com.android.chrome;end` 로 **크롬을 직접 연다**. 아이폰은 카톡 더보기(⋯) → 「다른 브라우저로 열기」 안내 + 주소 복사뿐 |
  | 안드로이드 크롬 | `beforeinstallprompt` 를 잡아 두면 **버튼 한 번으로 설치** |
  | 아이폰 Safari | 그런 API 가 **없다.** 공유 → 「홈 화면에 추가」 안내가 최선 |
  | 데스크톱 크롬 | 주소창 오른쪽 설치 아이콘 |

- ★`beforeinstallprompt` 는 **React 마운트보다 먼저 온다.** `index.html` 의 인라인
  스크립트에서 `window.__bip` 에 잡아 두고 `bip-ready` 를 쏜다. 컴포넌트에서만 듣게 하면
  이벤트를 놓쳐 버튼을 띄울 기회가 사라진다.
- **언제 뜨나 — 평소엔 아무한테도 안 뜬다.** ① 주소에 `?install=1`(공지용 링크)
  ② 인앱브라우저로 열었고 아직 설치 안 했을 때(한 번만). 설치본이면 무조건 안 뜬다.
- 공지에 뿌릴 주소: `https://hd-ship-location-238928524992.us-west1.run.app/?install=1`
- 검증: 카톡(안드/아이폰)·Safari·안드로이드 크롬·데스크톱·폴드커버 344px 로 17개 단언 통과.

### ★"아이콘을 꾹 눌러 바탕화면으로 끌어다 놓기" 는 아이폰에서 불가능하다
- 사용자 아이디어였고 좋은 발상이지만 **홈 화면은 URL 을 받는 드롭 대상이 아니다.**
  웹클립을 만드는 경로는 ① 공유 → 「홈 화면에 추가」 ② 단축어 앱의 "Make App from Web URL"
  둘뿐이고, ②는 오히려 단계가 늘어난다.
- 데스크톱은 링크를 바탕화면으로 끌 수 있다. **하지만 그건 북마크 바로가기**라 manifest 를
  안 읽어 제목·아이콘이 그 순간 값으로 굳는다 — 위의 "바로가기 만들기 vs 앱으로 설치" 표
  그대로다. **더 나쁜 결과**라 일부러 만들지 않았다.

### maskable 은 가운데 80% 만 산다
- 안드로이드가 기기 모양으로 잘라낸다. 그림을 80% 로 줄여 가운데 놓고 나머지는 바탕색.
- 한 파일을 `any` 와 `maskable` 로 겸용하면 둘 중 하나는 반드시 어색해진다. 따로 만든다.
- ★분리하는 이유는 **Safari 가 아니라 Chrome/안드로이드의 크롭**이다. WebKit 소스
  (`ApplicationManifestParser.cpp`)를 보면 `purpose` 를 공백으로 쪼개 합집합으로 담으므로
  `"any maskable"` 도 `any` 를 포함해 **Safari 기준으로는 실격이 아니다.** 옛 아이콘이
  안 나온 원인은 purpose 가 아니라 **apple-touch-icon 이 SVG 였던 것 하나뿐**이다.
  원인을 두 개로 세지 말 것.
- ★규격상 안전 영역은 사각형이 아니라 **지름 80% 짜리 원**이다(web.dev maskable-icon).
  사각 그림을 0.8 로 줄이면 네 모서리는 반지름 56.6% 지점이라 원형 마스크에 여전히
  잘린다. 지금은 모서리가 바탕색·파도뿐이라 실피해가 없지만, **모서리까지 그림을 채우는
  아이콘으로 바꾸면 `SAFE` 를 0.7 로 내리거나 그림을 원 안에 넣어야 한다.**

### ★없는 정적 파일이 200 으로 나가고 있었다 ⭐
- SPA 폴백(`try_files ... /index.html`)이 **모든** 미스를 index.html 로 넘겨서,
  아직 만들지도 않은 `/icon-180.png` 가 `200 · text/html · 1779B` 로 돌아왔다.
  파일명이 하나만 틀려도 브라우저는 "받았다" 로 알고 조용히 깨진 아이콘을 쓴다.
- 이미지·manifest 확장자는 `try_files $uri =404` 로 뺐다. js·css 는 넣지 말 것 —
  정규식 location 이 `/assets/` prefix 보다 먼저 잡혀 1년 캐시 설정을 덮어쓴다.
- 로컬 nginx 로 실제 응답을 확인하고 올린다(`nginx -t` 만으로는 이 문제가 안 보인다).

### 제목이 안 바뀌는 것은 배포 문제가 아니었다
- `pwa-check.yml` 로 프로덕션을 재 보면 title·apple-mobile-web-app-title·manifest 가
  전부 새 이름이다. **이미 설치된 바로가기·홈화면 아이콘은 원본이 바뀌어도 갱신되지 않는다** —
  지우고 다시 추가해야 한다. 확인은 추측 말고 그 액션으로 한다.
- **아이폰**: 옛 아이콘 길게 누르기 → `북마크 제거` → Safari 로 사이트 열고 당겨서 새로고침
  → 공유 → `홈 화면에 추가`.
- **PC(크롬)**: 바탕화면 바로가기 삭제 → `chrome://apps` 에서 옛 앱 제거 → 사이트에서
  `Ctrl+Shift+R` → 주소창 오른쪽 설치 아이콘으로 다시 설치.
  크롬은 **이미 설치된 앱의 `name`·`short_name`·`icons` 변경을 자동 반영하지 않는다**
  (공식 문서 명시. `display`·`scope`·`start_url`·`theme_color` 등만 갱신된다).
  `manifest.json` 에 `id: "/"` 를 넣어 뒀으므로 재설치해도 같은 앱으로 인식된다.
- 이름은 네 곳(`<title>`·`apple-mobile-web-app-title`·manifest `name`·`short_name`)을
  **항상 같게** 둔다. 바꿀 일이 생기면 한 커밋에서 넷을 같이 고친다 — `pwa-check` 가
  네 값이 어긋나면 실패시킨다.
  (참고: 홈화면 캡션은 12자 안팎에서 잘린다. 지금 이름은 16자라 잘려 보인다.
   짧은 이름을 쓰고 싶으면 `short_name` 만 줄이면 되는데, **이름은 사용자가 정한다** —
   내가 임의로 줄이지 않는다.)

### ★절대 바꾸면 안 되는 것 — `id` · `start_url` · `manifest.json` 경로 ⭐
- manifest 의 `id`·`start_url` 은 **설치본의 신원**이다. 바꾸는 순간 브라우저가 아예 다른
  앱으로 보고, 폰·PC 에 깔린 앱이 고아가 되어 이름·아이콘이 영영 옛 것으로 남고
  새로 설치하면 두 개가 생긴다. 지금 `id: "/"` 는 id 가 없던 시절의 암묵 신원
  (`start_url`)과 **같은 값**이라 기존 설치본을 안 끊는다. 그대로 둔다.
- `manifest.json` 의 **파일명·경로**도 바꾸지 말 것. 브라우저가 갱신을 못 하게 될 수 있다.
- 아이콘 `src` 를 해시로 바꾸는 건 안전하다(오히려 갱신 트리거). `id` 를 해시나 버전으로
  만드는 것만 금지.

### "바로가기 만들기" 와 "앱으로 설치" 는 완전히 다른 것이다 (윈도우 크롬)
| | 이름의 출처 | 아이콘의 출처 | 나중에 갱신 |
|---|---|---|---|
| **바로가기 만들기** | 그 순간의 `<title>` (대화상자에서 수정 가능) | `<link rel=icon>` 목록 **manifest 안 읽음** | 안 됨 — .lnk 에 굳는다 |
| **앱으로 설치** | manifest `name` | manifest `icons` | 승인 후 반영 |
- 그래서 `index.html` 에 **192px PNG `rel=icon`** 을 같이 둔다. 32px 만 두면 바탕화면에서
  늘어나 뭉개지고, 하나도 못 고르면 크롬이 제목 첫 글자로 그림을 그려 버린다.
- 런타임에 `document.title` 을 바꾸지 말 것 — 그 순간의 값이 바로가기 이름으로 굳는다.
- 진단: PC 크롬 `chrome://web-app-internals` 에서 저장된 name 을 직접 본다.
  안드로이드는 `about://webapks`. 저장된 name 이 새 이름인데 바탕화면만 옛 이름이면
  그건 앱이 아니라 **북마크 바로가기**다.

### `no-cache` 는 "저장 금지" 가 아니라 "쓰기 전에 재검증" 이다
- 서버 헤더가 완벽해도 브라우저·설치된 앱은 로컬 복사본을 들고 있다. 설치된 PWA 의
  manifest 사본은 HTTP 캐시와 **다른 층**이다(최대 24시간 스로틀 + 앱 창을 다 닫아야 적용).
- **서버가 맞는데 화면이 옛것이면 다음 층(설치 스냅샷)을 봐라.** nginx 를 또 고치지 말 것.
- ★Cloud Run 은 응답에 `Set-Cookie` 가 있으면 `Cache-Control` 을 **`private` 로 바꿔 버린다.**
  지금은 쿠키를 안 내보내 무사한데, 세션 어피니티 같은 걸 켜면 조용히 깨진다.
  그래서 `pwa-check` 가 `/` 와 `/manifest.json` 의 `no-cache` 를 검사한다.

### pwa-check 는 찍기만 하지 않고 실패시킨다
- 아이콘 파일명을 액션에 박아 두지 않는다 — 해시가 붙어 있어 그림을 바꾸면 이름이 바뀐다.
  프로덕션 index.html·manifest 가 **실제로 가리키는** 경로만 훑는다.
- 검사 셋: ① 참조된 아이콘이 전부 `200 + image/*` ② `apple-touch-icon` 이 PNG
  ③ 없는 png 가 404 ④ 이름 네 곳이 동일. 하나라도 어긋나면 **빨간불**.
- ★검사는 "고의로 망가뜨린 dist" 로 실제로 빨간불이 뜨는지 확인하고 올렸다.
  안 그러면 통과가 아무 뜻도 없다.
- ★러너 셸은 `bash -e` 다. `grep` 이 못 찾으면 스텝이 통째로 죽으므로 추출용 파이프라인·
  명령치환에는 전부 `|| true` 를 붙인다. 경로에 한글을 쓰지 말 것(curl 이 그대로 못 보낸다).

## 🖥️ 터미널 (Windows 기준)
- **cmd 만 사용, PowerShell 금지.** `$env:` 같은 PS 문법으로 안내하지 말 것.
- 디렉터리 이동은 `cd /d <경로>` (드라이브 달라도 한 번에 이동).
- macOS·리눅스·클라우드 세션에서는 일반 셸을 쓴다.
